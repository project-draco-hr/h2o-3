{
switch (_link) {
case logit:
    double div=(x * (1 - x));
  if (div < 1e-6)   return 1e6;
return 1.0 / div;
case identity:
return 1;
case log:
return 1.0 / x;
case inverse:
return -1.0 / (x * x);
default :
throw H2O.unimpl();
}
}
