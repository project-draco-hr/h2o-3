{
switch (_output.getModelCategory()) {
case Binomial:
    return new GLMMetricsBuilderBinomial(domain,ModelUtils.DEFAULT_THRESHOLDS);
case Regression:
  return new ModelMetricsRegression.MetricBuilderRegression();
default :
throw H2O.unimpl();
}
}
