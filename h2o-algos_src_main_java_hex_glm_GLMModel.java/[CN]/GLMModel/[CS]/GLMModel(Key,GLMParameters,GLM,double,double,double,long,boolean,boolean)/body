{
  super(selfKey,parms,null);
  _ymu=ymu;
  _ySigma=ySigma;
  _lambda_max=lambda_max;
  _nobs=nobs;
  if (_parms._family == Family.multinomial)   _output=job == null ? new GLMOutputMultinomial() : new GLMOutputMultinomial(job);
 else   _output=job == null ? new GLMOutput() : new GLMOutput(job);
}
