{
  double[][] res=new double[nclasses()][];
  Submodel sm=_submodels[_best_lambda_idx];
  for (int i=0; i < res.length; ++i)   if (sm.idxs == null)   res[i]=sm.betaMultinomial[i].clone();
 else   throw H2O.unimpl();
  return res;
}
