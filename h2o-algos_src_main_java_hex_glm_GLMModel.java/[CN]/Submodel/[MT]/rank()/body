{
  if (betaMultinomial != null) {
    int res=0;
    for (    double[] ds : betaMultinomial)     for (    double d : ds)     if (d != 0)     ++res;
    return res;
  }
  return idxs != null ? idxs.length : (ArrayUtils.countNonzeros(beta));
}
