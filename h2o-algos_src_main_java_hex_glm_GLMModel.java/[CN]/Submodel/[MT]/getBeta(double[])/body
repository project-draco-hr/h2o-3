{
  if (idxs != null) {
    for (int i=0; i < idxs.length; ++i)     beta[idxs[i]]=this.beta[i];
    beta[beta.length - 1]=this.beta[this.beta.length - 1];
  }
 else   System.arraycopy(this.beta,0,beta,0,beta.length);
  return beta;
}
