{
  boolean bail=false;
  if (p._locked.contains(f._key)) {
    subRefLocked(f);
    bail=true;
  }
  for (  Key k : f.keys()) {
    if (!p._locked.contains(k)) {
      subRefLocked((Vec)DKV.getGet(k));
      bail=true;
    }
  }
  return bail;
}
