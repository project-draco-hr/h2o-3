{
  String res=null;
  if (_local != null)   res=_local.valueOf(name);
  if (res == null && search_global)   res=_global.valueOf(name);
  if (res == null)   res=_parent.getValue(name,false);
  if (res == null)   throw H2O.fail("Failed lookup of variable: " + name);
  return res;
}
