{
  _refcnt=new IcedHashMap<>();
  _stack=new ExecStack();
  _trash=new HashSet<>();
  int len=ab.get4();
  for (int i=0; i < len; ++i) {
    _refcnt.put((Vec)DKV.getGet(ab.getStr()),new IcedInt(ab.get4()));
  }
  return this;
}
