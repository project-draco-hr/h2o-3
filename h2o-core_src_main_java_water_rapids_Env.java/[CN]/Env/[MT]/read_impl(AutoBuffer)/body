{
  _refcnt=new HashMap<>();
  _stack=new ExecStack();
  _trash=new HashSet<>();
  int len=ab.get4();
  for (int i=0; i < len; ++i) {
    _refcnt.put(ab.get(Vec.class),new IcedInt(ab.get4()));
  }
  return this;
}
