{
  _globals=new HashSet<>();
  int len=ab.get4();
  for (int i=0; i < len; i++)   _globals.add((Vec)DKV.getGet((Key)ab.get()));
  _refcnt=new ArrayList<>();
  len=ab.get4();
  for (int i=0; i < len; i++)   _refcnt.add((Frame)ab.get());
  return this;
}
