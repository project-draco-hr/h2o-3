{
  if (o instanceof ValFrame) {
    if (_local != null) {
      String key=Key.make().toString();
      _local._local_frames.put(key,((ValFrame)o)._fr);
    }
  }
  if (o instanceof ValFrame) {
    addRef(o);
  }
  _stack.push(o);
}
