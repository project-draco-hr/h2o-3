{
  while (!_stack.isEmpty()) {
    int type=peekType();
switch (type) {
case ARY:
      remove(peek(),false);
    break;
default :
  pop();
break;
}
}
Futures fs=new Futures();
for (Key k : _refcnt.keySet()) if (_refcnt.get(k)._val == 0) removeVec(((Vec)DKV.getGet(k)),fs);
fs.blockForPending();
for (Frame f : _tmpFrames) {
if (f._key != null) DKV.remove(f._key);
}
}
