{
  org.apache.log4j.Logger l=LogManager.getLogger(water.api.RequestServer.class);
  String s=String.format("  %-10s  %s","tid(" + Long.toString(Thread.currentThread().getId()) + ")",msg);
  l.info(s);
}
