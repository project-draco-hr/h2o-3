{
  String tree="(&& (KEY a.hex) (KEY a.hex))";
  checkTree(tree);
  tree="(|| (KEY a.hex) (KEY a.hex))";
  checkTree(tree);
  tree="(& (KEY a.hex) (KEY a.hex))";
  checkTree(tree);
  tree="(| (KEY a.hex) (KEY a.hex))";
  checkTree(tree);
}
