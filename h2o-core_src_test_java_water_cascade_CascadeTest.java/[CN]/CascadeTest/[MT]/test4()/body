{
  String tree="(n $a.hex #5)";
  checkTree(tree);
  tree="(L $a.hex #5)";
  checkTree(tree);
  tree="(G $a.hex #1.25132)";
  checkTree(tree);
  tree="(g $a.hex #112.341e-5)";
  checkTree(tree);
  tree="(l $a.hex #0.0123)";
  checkTree(tree);
  tree="(N $a.hex #0)";
  checkTree(tree);
  checkTree(tree);
}
