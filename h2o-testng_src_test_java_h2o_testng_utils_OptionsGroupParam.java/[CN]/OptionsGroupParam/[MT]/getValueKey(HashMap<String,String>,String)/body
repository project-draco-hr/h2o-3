{
  if (!"".equals(rawInput.get(key).trim())) {
    try {
      return values[optionsGroup.indexOf(rawInput.get(key).trim())];
    }
 catch (    Exception ex) {
      return null;
    }
  }
  return null;
}
