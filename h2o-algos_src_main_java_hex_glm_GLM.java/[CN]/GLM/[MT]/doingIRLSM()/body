{
  if (_parms._family == Family.gaussian && _parms._link == Link.identity)   return false;
  if (_parms._solver == Solver.IRLSM)   return true;
  if (_parms._solver == Solver.AUTO)   return true;
  return false;
}
