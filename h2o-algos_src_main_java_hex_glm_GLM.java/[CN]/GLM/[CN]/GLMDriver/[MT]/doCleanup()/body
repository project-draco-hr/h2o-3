{
  try {
    _model.unlock(_job);
  }
 catch (  Throwable t) {
  }
  try {
    _parms.read_unlock_frames(_job);
  }
 catch (  Throwable t) {
  }
  Scope.exit(_keys2Keep.values().toArray(new Key[0]));
}
