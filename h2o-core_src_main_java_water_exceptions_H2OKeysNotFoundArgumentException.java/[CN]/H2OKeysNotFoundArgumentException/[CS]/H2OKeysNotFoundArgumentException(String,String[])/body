{
  super("Keys not found: " + argument + ": "+ names.toString(),"Key not found: " + argument + ": "+ names.toString());
  this.values=new IcedHashMap<String,Object>();
  this.values.put("argument",argument);
  this.values.put("names",names);
}
