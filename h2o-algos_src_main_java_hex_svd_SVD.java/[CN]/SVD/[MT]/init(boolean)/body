{
  super.init(expensive);
  if (_parms._max_iterations < 1)   error("_max_iterations","max_iterations must be at least 1");
  if (_train != null && (_parms._k < 1 || _parms._k > _train.numCols()))   error("_k","Number of singular values must be between 1 and " + _train.numCols());
}
