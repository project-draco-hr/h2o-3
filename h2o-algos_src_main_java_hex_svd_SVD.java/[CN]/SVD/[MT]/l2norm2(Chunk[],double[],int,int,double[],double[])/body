{
  double sumsqr=0;
  for (int row=0; row < cs[0]._len; row++) {
    double sum=0;
    for (int j=0; j < ncols; j++) {
      double a=cs[j].atd(row);
      sum+=(a - normSub[j]) * normMul[j] * vec[j];
    }
    sumsqr+=sum * sum;
    chk_u(cs,k,ncols).set(row,sum);
  }
  return sumsqr;
}
