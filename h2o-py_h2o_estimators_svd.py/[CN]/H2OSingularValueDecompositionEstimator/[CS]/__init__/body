def __init__(self, **kwargs):
    super(H2OSingularValueDecompositionEstimator, self).__init__()
    self._parms = {}
    for name in ['model_id', 'training_frame', 'validation_frame', 'ignored_columns', 'ignore_const_cols', 'score_each_iteration', 'transform', 'svd_method', 'nv', 'max_iterations', 'seed', 'keep_u', 'u_name', 'use_all_factor_levels', 'max_runtime_secs']:
        pname = (name[:(-1)] if (name[(-1)] == '_') else name)
        self._parms[pname] = (kwargs[name] if (name in kwargs) else None)
