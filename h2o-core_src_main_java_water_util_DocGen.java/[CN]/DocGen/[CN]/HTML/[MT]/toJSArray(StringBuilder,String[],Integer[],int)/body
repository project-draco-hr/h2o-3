{
  sb.append('[');
  for (int i=0; i < maxValues; i++) {
    if (i > 0)     sb.append(',');
    sb.append('"').append(ss[sortOrder != null ? sortOrder[i] : i]).append('"');
  }
  sb.append(']');
  return sb;
}
