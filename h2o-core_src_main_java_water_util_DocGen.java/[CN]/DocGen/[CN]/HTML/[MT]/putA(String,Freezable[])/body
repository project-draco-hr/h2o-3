{
  if (fs == null)   return f(name,"null");
  f0(name).arrayHead();
  for (  Freezable f : fs) {
    p("<tr><td>");
    if (f != null)     f.writeHTML(this);
    p("</td></tr>");
  }
  return arrayTail().f1();
}
