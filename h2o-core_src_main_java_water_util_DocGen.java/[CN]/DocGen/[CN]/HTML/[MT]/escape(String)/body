{
  int len=s.length();
  for (int i=0; i < len; i++) {
    char c=s.charAt(i);
    if (c == '<')     p("&lt;");
 else     if (c == '>')     p("&gt;");
 else     if (c == '&')     p("&amp;");
 else     if (c == '"')     p("&quot;");
 else     p(c);
  }
  return this;
}
