{
  ParseSetup setup=new ParseSetup(true,0,0,null,parse.pType,parse.sep,parse.ncols,parse.singleQuotes,parse.columnNames,parse.domains,null,parse.checkHeader,null);
  Key[] srcs=new Key[parse.srcs.length];
  for (int i=0; i < parse.srcs.length; i++)   srcs[i]=parse.srcs[i].key;
  parse.job=(JobV2)Schema.schema(version,Job.class).fillFromImpl(water.parser.ParseDataset2.startParse2(parse.hex,srcs,parse.delete_on_done,setup));
  return parse;
}
