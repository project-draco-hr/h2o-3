{
  if (_vocabHM == null)   return ab.put1(1);
  int strLen=0;
  for (  ValueStringCount val : VOCABHM.values())   strLen+=val.get_length();
  ab.put1(0);
  ab.put4(strLen);
  for (  ValueStringCount val : VOCABHM.values())   ab.put2((char)val.get_length()).putA1(val.get_buf(),val.get_off(),val.get_off() + val.get_length()).put8(val._cnt);
  return ab.put2((char)65535);
}
