{
  if (value == null) {
    return false;
  }
  String bValue=value.trim().toLowerCase();
  if ("x".equals(bValue) || "y".equals(bValue) || "yes".equals(bValue)) {
    return true;
  }
  return false;
}
