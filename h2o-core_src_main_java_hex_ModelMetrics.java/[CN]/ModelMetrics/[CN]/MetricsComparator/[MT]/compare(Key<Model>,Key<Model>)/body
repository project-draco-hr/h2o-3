{
  double c1=getMetricFromModel(key1,_sort_by);
  double c2=getMetricFromModel(key2,_sort_by);
  if (decreasing) {
    return (c2 - c1 > 0 ? 1 : -1);
  }
 else {
    return (c1 - c2 > 0 ? 1 : -1);
  }
}
