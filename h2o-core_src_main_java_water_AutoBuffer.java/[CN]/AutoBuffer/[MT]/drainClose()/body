{
  try {
    try {
      Log.info("drainClose channel to " + ((SocketChannel)_chan).socket().getInetAddress());
    }
 catch (    Throwable t) {
      Log.info("drainClose channel to unknown node");
    }
    _chan.close();
    restorePriority();
    bbFree();
  }
 catch (  IOException e) {
    throw Log.throwErr(e);
  }
}
