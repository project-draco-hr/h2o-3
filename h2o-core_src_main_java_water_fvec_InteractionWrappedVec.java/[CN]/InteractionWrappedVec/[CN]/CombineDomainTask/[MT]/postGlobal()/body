{
  Arrays.sort(_dom=_perChkMap.keySet().toArray(new String[_perChkMap.size()]));
  int idx=0;
  _bins=new long[_perChkMap.size()];
  for (  IcedLong il : _perChkMap.values())   _bins[idx++]=il._val;
}
