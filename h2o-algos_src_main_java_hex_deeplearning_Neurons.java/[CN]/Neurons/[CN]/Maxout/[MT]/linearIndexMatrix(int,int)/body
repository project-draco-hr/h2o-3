{
  final int cols=_previous._a.size();
  final int idx=row * cols + col;
  assert(_maxIncoming[row] == 0 || _maxIncoming[row] == 1);
  assert(_maxIncoming[row] == 0);
  return _k * idx + _maxIncoming[row];
}
