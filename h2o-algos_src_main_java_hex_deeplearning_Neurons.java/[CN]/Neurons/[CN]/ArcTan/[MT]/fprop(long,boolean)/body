{
  gemv(_a,_w,_previous._a,_b,_dropout != null ? _dropout.bits() : null);
  final int rows=_a.size();
  for (int row=0; row < rows; row++)   _a.set(row,Math.atan(_a.get(row)) * 2 / Math.PI);
  compute_sparsity();
}
