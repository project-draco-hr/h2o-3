{
  Matrix m=null;
  if (dcm != null)   m=dcm;
  if (drm != null)   m=drm;
  if (scm != null)   m=scm;
  if (srm != null)   m=srm;
  for (int c=0; c < cs.length; ++c) {
    for (int r=0; r < m.rows(); ++r) {
      cs[c].set(r,m.get((int)cs[0].start() + r,c));
    }
  }
}
