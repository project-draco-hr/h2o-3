{
  assert(target != missing_real_value);
  final int row=0;
  final float t=target;
  final float y=_a.get(row);
  float g=(float)new Distribution(params._distribution,params._tweedie_power).gradient(t,y);
  _sumGrad+=g;
}
