{
  parms._seed=seed;
  KMeans job=new KMeans(parms);
  KMeansModel kmm=job.get();
  job.remove();
  for (int i=0; i < parms._K; i++)   AssertJUnit.assertTrue("Seed: " + seed,kmm._rows[i] != 0);
  return kmm;
}
