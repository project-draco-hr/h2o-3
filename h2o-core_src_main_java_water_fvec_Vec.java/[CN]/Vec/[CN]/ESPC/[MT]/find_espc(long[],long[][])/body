{
  for (int i=0; i < espcs.length; i++)   if (espc == espcs[i])   return i;
  for (int i=0; i < espcs.length; i++)   if (espc.length == espcs[i].length && Arrays.equals(espc,espcs[i]))   return i;
  return -1;
}
