{
  for (int i=0; i < c._len; ++i)   if (_domain == null)   nc.addNum(c.at8(i));
 else {
    long num=Arrays.binarySearch(_domain,c.at8(i));
    if (num < 0)     System.out.println("asdf");
    nc.addNum(num);
  }
}
