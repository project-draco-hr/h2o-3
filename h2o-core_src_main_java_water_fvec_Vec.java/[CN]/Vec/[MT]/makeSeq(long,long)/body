{
  return new MRTask(){
    @Override public void map(    Chunk[] cs){
      for (      Chunk c : cs)       for (int r=0; r < c._len; r++)       c.set0(r,r + min + c._start);
    }
  }
.doAll(makeZero(len))._fr.vecs()[0];
}
