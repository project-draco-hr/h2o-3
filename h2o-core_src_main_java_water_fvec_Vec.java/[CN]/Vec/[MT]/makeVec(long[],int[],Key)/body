{
  long[] espc=new long[2];
  espc[1]=ms.length;
  Vec v=new Vec(vecKey,espc);
  NewChunk nc=new NewChunk(v,0);
  Futures fs=new Futures();
  for (int i=0; i < ms.length; ++i)   nc.addNum(ms[i],es[i]);
  nc.close(fs);
  DKV.put(v._key,v,fs);
  fs.blockForPending();
  return v;
}
