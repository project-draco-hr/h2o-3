{
  Vec gausVec=makeZero();
  new MRTask(){
    @Override public void map(    Chunk c){
      Random rng=RandomUtils.getRNG(seed * (c.cidx() + 1));
      for (int i=0; i < c._len; ++i)       c.set(i,rng.nextGaussian());
    }
  }
.doAll(gausVec);
  return gausVec;
}
