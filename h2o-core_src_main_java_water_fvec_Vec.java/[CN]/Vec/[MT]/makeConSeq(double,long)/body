{
  int chunks=(int)Math.ceil((double)len / Vec.CHUNK_SZ);
  long[] espc=new long[chunks + 1];
  for (int i=1; i <= chunks; ++i)   espc[i]=Math.min(espc[i - 1] + Vec.CHUNK_SZ,len);
  return new Vec(VectorGroup.VG_LEN1.addVec(),espc).makeCon(x);
}
