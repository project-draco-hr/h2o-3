{
  int nchunks=(int)Math.max(1,len >> DFLT_LOG2_CHUNK_SIZE);
  long[] espc=new long[nchunks + 1];
  for (int i=0; i < nchunks; i++)   espc[i]=((long)i) << DFLT_LOG2_CHUNK_SIZE;
  espc[nchunks]=len;
  return makeCon(x,VectorGroup.VG_LEN1,espc);
}
