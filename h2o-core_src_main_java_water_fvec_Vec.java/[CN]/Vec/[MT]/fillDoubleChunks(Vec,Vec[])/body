{
  new MRTask(){
    public void map(    Chunk c){
      for (int i=0; i < ds.length; ++i)       DKV.put(ds[i].chunkKey(c.cidx()),new C8DChunk(MemoryManager.malloc1(c._len << 3)));
    }
  }
.doAll(v);
}
