{
  int nchunks=(int)Math.max(1,(len >> LOG_CHK) - 1);
  long[] espc=new long[nchunks + 1];
  for (int i=0; i < nchunks; i++)   espc[i]=((long)i) << LOG_CHK;
  espc[nchunks]=len;
  return makeCon(l,domain,VectorGroup.VG_LEN1,espc);
}
