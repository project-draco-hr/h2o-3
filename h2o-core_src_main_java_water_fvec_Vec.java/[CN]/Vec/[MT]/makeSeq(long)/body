{
  return new MRTask(){
    @Override public void map(    Chunk[] cs){
      for (int i=0; i < cs.length; i++) {
        Chunk c=cs[i];
        for (int r=0; r < c.len(); r++)         c.set0(r,r + 1 + c._start);
      }
    }
  }
.doAll(makeConSeq(0,len))._fr.vecs()[0];
}
