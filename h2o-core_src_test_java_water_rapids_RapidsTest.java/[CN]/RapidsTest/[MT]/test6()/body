{
  String tree="(cols a.hex [0])";
  checkTree(tree);
  tree="(rows (cols a.hex [0]) [5])";
  checkTree(tree);
  tree="(cols (rows a.hex [0:4 6 7]) [0])";
  checkTree(tree);
  tree="(rows a.hex [2 7 8])";
  checkTree(tree);
}
