{
  String tree="(== a.hex #5)";
  checkTree(tree);
  tree="(<= a.hex #5)";
  checkTree(tree);
  tree="(>= a.hex #1.25132)";
  checkTree(tree);
  tree="(< a.hex #112.341e-5)";
  checkTree(tree);
  tree="(> a.hex #0.0123)";
  checkTree(tree);
  tree="(!= a.hex #0)";
  checkTree(tree);
}
