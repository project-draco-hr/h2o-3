{
  Job j=SQLManager.importSqlTable(importSqlTable.connection_url,importSqlTable.table,importSqlTable.username,importSqlTable.password,importSqlTable.optimize);
  return new JobV3().fillFromImpl(j);
}
