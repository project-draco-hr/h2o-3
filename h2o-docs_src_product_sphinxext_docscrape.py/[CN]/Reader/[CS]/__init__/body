def __init__(self, data):
    "\n        Parameters\n        ----------\n        data : str\n           String with lines separated by '\n'.\n\n        "
    if isinstance(data, list):
        self._str = data
    else:
        self._str = data.split('\n')
    self.reset()
