{
  if (_vecs == null)   return 0;
  int cols=0;
  for (int i=0; i < _vecs.length; i++) {
    if (_vecs[i].isCategorical() && _vecs[i].domain() != null)     cols+=_vecs[i].domain().length - (useAllFactorLevels ? 0 : 1) + (missingBucket ? 1 : 0);
 else     cols++;
  }
  return cols;
}
