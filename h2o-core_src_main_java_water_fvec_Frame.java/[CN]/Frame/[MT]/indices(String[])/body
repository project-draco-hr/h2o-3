{
  if (_names == null)   return null;
  Map<String,Integer> names_map=new HashMap();
  for (int i=0; i < _names.length; i++)   names_map.put(_names[i],i);
  int[] res=new int[cols.length];
  for (int i=0; i < cols.length; ++i)   res[i]=names_map.get(i);
  return res;
}
