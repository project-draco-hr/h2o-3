import sys
import h2o
from h2o import H2ODeeplearning
if (__name__ == '__main__'):
    args = sys.argv
    print args
    if (len(args) > 1):
        ip = args[1]
        port = int(args[2])
    else:
        ip = 'localhost'
        port = 54321
    deep_learning_metrics_test(ip, port)
