import sys
import h2o
from h2o import H2ODeeplearning
ip = 'localhost'
port = 54321
if (__name__ == '__main__'):
    args = sys.argv
    global ip
    global port
    if (len(args) > 1):
        ip = args[1]
        port = args[2]
    deep_learning_metrics_test()
