{
  _nrows=chks[0]._len;
  _sparse=sparse;
  if (sparse) {
    _denseRow=null;
    _chks=null;
    _sparseRows=extractSparseRows(chks,0);
  }
 else {
    _denseRow=DataInfo.this.newDenseRow();
    _chks=chks;
    _sparseRows=null;
  }
}
