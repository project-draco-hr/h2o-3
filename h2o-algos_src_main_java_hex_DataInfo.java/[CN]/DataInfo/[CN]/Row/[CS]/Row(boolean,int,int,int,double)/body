{
  binIds=MemoryManager.malloc4(eBins);
  numVals=MemoryManager.malloc8d(eNums);
  response=MemoryManager.malloc8d(nresponses);
  if (sparse)   numIds=MemoryManager.malloc4(eNums);
  this.etaOffset=etaOffset;
}
