{
  if (_catLvls != null) {
    int c=Arrays.binarySearch(_catLvls[cid],val);
    if (c >= 0)     return c + _catOffsets[cid];
  }
 else   if (val != 0 || _useAllFactorLevels)   return val + _catOffsets[cid];
  return -1;
}
