def _eager(self):
    if (not self._computed):
        aggs = []
        for k in self._aggs:
            aggs += self._aggs[k]
        self._res = h2o.H2OFrame._expr(expr=h2o.ExprNode('GB', self._fr, self._by, *aggs))._frame()
        self._computed = True
