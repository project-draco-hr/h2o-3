def to_bytes(s):
    u'Convert string s into bytes (assuming utf-8 encoding).'
    if (PY3 or isinstance(s, native_unicode)):
        return s.encode(u'utf-8')
    else:
        return native_unicode(s).encode(u'utf-8')
