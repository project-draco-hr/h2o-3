def is_numeric(x):
    u'Test whether the provided argument is either an integer or a float.'
    if PY2:
        return isinstance(x, (int, native_long, float))
    else:
        return isinstance(x, (int, float))
