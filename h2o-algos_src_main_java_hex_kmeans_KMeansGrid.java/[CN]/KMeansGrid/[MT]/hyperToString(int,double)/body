{
switch (h) {
case 0:
    return Double.toString(val);
case 1:
  return val == 0 ? "false" : "true";
case 2:
return KMeans.Initialization.values()[(int)val].toString();
case 3:
return Long.toString((long)val);
default :
throw H2O.fail();
}
}
