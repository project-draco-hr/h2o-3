{
  if (hasNext()) {
    if (_x + 1 >= _ast.length)     return false;
    if (_x + 2 >= _ast.length)     return false;
    if (_ast[_x] == ';' && _ast[_x + 1] == ';' && _ast[_x + 2] == ';')     return false;
    return true;
  }
  return false;
}
