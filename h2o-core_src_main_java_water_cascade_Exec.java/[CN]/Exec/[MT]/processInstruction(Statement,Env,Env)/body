{
switch (s.kind()) {
case Program.OP:
    runOp(s.op(),global,local);
  break;
case Program.CALL:
runCall(s.name(),global);
break;
case Program.PUSH:
{
if (local != null) {
local.push(s.value());
}
 else global.push(s.value());
break;
}
}
}
