{
  Log.info("Removing all objects");
  Futures fs=new Futures();
  for (  Job j : Job.jobs()) {
    j.stop_requested();
    j.remove(fs);
  }
  if (InitIDHandler.SESSIONS != null) {
    for (    String k : InitIDHandler.SESSIONS.keySet())     (InitIDHandler.SESSIONS.get(k)).endQuietly(null);
    InitIDHandler.SESSIONS.clear();
  }
  fs.blockForPending();
  new MRTask(H2O.MIN_HI_PRIORITY){
    @Override public void setupLocal(){
      H2O.raw_clear();
      water.fvec.Vec.ESPC.clear();
    }
  }
.doAllNodes();
  H2O.getPM().getIce().cleanUp();
  Log.info("Finished removing objects");
  return u;
}
