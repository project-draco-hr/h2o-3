@property
def elastic_averaging_moving_rate(self):
    u'float: Elastic averaging moving rate (only if elastic averaging is enabled). (Default: 0.9)'
    return self._parms.get(u'elastic_averaging_moving_rate')
