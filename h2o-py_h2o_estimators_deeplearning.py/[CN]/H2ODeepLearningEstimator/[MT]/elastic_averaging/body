@property
def elastic_averaging(self):
    u'\n        bool: Elastic averaging between compute nodes can improve distributed model convergence. #Experimental (Default:\n        False)\n        '
    return self._parms.get(u'elastic_averaging')
