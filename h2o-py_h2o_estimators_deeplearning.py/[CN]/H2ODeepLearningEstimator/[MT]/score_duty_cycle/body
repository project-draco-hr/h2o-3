@property
def score_duty_cycle(self):
    u'\n        float: Maximum duty cycle fraction for scoring (lower: more training, higher: more scoring). (Default: 0.1)\n        '
    return self._parms.get(u'score_duty_cycle')
