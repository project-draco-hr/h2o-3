@property
def hidden_dropout_ratios(self):
    u'\n        List[float]: Hidden layer dropout ratios (can improve generalization), specify one value per hidden layer,\n        defaults to 0.5.\n        '
    return self._parms.get(u'hidden_dropout_ratios')
