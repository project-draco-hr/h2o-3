@property
def rate_decay(self):
    u'\n        float: Learning rate decay factor between layers (N-th layer: rate * rate_decay ^ (n - 1). (Default: 1.0)\n        '
    return self._parms.get(u'rate_decay')
