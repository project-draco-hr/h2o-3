@property
def max_hit_ratio_k(self):
    u'\n        int: Max. number (top K) of predictions to use for hit ratio computation (for multi-class only, 0 to disable).\n        (Default: 0)\n        '
    return self._parms.get(u'max_hit_ratio_k')
