@property
def variable_importances(self):
    u'\n        bool: Compute variable importances for input features (Gedeon method) - can be slow for large networks.\n        (Default: False)\n        '
    return self._parms.get(u'variable_importances')
