@property
def balance_classes(self):
    u'\n        bool: Balance training data class counts via over/under-sampling (for imbalanced data). (Default: False)\n        '
    return self._parms.get(u'balance_classes')
