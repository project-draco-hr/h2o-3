@property
def seed(self):
    u'\n        int: Seed for random numbers (affects sampling) - Note: only reproducible when running single threaded.\n        (Default: -1)\n        '
    return self._parms.get(u'seed')
