@property
def elastic_averaging_regularization(self):
    u'\n        float: Elastic averaging regularization strength (only if elastic averaging is enabled). (Default: 0.001)\n        '
    return self._parms.get(u'elastic_averaging_regularization')
