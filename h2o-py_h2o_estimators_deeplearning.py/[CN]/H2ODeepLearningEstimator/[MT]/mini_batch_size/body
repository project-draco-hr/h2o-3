@property
def mini_batch_size(self):
    u'\n        int: Mini-batch size (smaller leads to better fit, larger can speed up and generalize better). (Default: 1)\n        '
    return self._parms.get(u'mini_batch_size')
