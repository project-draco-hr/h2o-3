@property
def overwrite_with_best_model(self):
    u'\n        bool: If enabled, override the final model with the best model found during training. (Default: True)\n        '
    return self._parms.get(u'overwrite_with_best_model')
