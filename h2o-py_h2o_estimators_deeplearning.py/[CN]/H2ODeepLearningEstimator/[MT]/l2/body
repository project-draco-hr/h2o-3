@property
def l2(self):
    u'\n        float: L2 regularization (can add stability and improve generalization, causes many weights to be small.\n        (Default: 0.0)\n        '
    return self._parms.get(u'l2')
