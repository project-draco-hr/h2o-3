@property
def classification_stop(self):
    u'\n        float: Stopping criterion for classification error fraction on training data (-1 to disable). (Default: 0.0)\n        '
    return self._parms.get(u'classification_stop')
