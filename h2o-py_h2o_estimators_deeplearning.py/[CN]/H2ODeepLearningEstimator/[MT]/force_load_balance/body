@property
def force_load_balance(self):
    u'\n        bool: Force extra load balancing to increase training speed for small datasets (to keep all cores busy).\n        (Default: True)\n        '
    return self._parms.get(u'force_load_balance')
