@property
def col_major(self):
    u'\n        bool: #DEPRECATED Use a column major weight matrix for input layer. Can speed up forward propagation, but might\n        slow down backpropagation. (Default: False)\n        '
    return self._parms.get(u'col_major')
