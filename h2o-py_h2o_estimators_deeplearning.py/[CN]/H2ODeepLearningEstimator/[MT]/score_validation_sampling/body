@property
def score_validation_sampling(self):
    u'\n        Enum["uniform", "stratified"]: Method used to sample validation dataset for scoring. (Default: "uniform")\n        '
    return self._parms.get(u'score_validation_sampling')
