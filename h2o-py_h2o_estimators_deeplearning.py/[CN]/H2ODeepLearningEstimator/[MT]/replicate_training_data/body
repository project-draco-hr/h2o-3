@property
def replicate_training_data(self):
    u'\n        bool: Replicate the entire training dataset onto every node for faster training on small datasets. (Default:\n        True)\n        '
    return self._parms.get(u'replicate_training_data')
