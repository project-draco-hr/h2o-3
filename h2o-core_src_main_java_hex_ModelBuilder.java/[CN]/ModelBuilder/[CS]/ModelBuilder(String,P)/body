{
  super((parms._destination_key == null ? Key.make(desc + "Model_" + Key.rand()) : parms._destination_key),desc);
  _parms=parms;
  if (parms.sanityCheckParameters() > 0)   throw new IllegalArgumentException("Error(s) in model parameters: " + parms.validationErrors());
}
