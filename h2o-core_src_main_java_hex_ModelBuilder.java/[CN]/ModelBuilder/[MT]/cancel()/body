{
  super.cancel();
  if (_cvModelBuilderKeys != null) {
    for (int i=0; i < _cvModelBuilderKeys.length; ++i) {
      ModelBuilder<M,P,O> mb=DKV.getGet(_cvModelBuilderKeys[i]);
      if (mb != null) {
        assert(mb._cvModelBuilderKeys == null);
        mb.cancel();
      }
    }
  }
}
