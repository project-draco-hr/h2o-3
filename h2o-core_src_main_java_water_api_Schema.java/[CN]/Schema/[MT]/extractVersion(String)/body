{
  if (null == _version_pattern)   _version_pattern=Pattern.compile(".*V(\\d+)");
  Matcher m=_version_pattern.matcher(clz_name);
  if (!m.matches())   return -1;
  return Integer.valueOf(m.group(1));
}
