{
  super.setupLocal();
  if (consensusADMM) {
    if (DKV.get(_sharedmodel.myModelInfoKey(H2O.SELF)) != null) {
      _mymodel=DKV.getGet(_sharedmodel.myModelInfoKey(H2O.SELF));
    }
 else {
      _mymodel=_sharedmodel;
    }
  }
 else {
    _mymodel=_sharedmodel;
    _sharedmodel=null;
  }
}
