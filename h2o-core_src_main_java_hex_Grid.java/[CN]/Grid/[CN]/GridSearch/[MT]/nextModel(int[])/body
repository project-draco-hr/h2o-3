{
  int i;
  for (i=0; i < hidx.length; i++)   if (hidx[i] + 1 < _hyperSearch[i].length)   break;
  if (i == hidx.length)   return null;
  for (int j=0; j < i; j++)   hidx[j]=0;
  hidx[i]++;
  return hidx;
}
