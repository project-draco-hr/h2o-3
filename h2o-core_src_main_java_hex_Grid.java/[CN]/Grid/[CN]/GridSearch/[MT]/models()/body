{
  Model[] ms=new Model[_total_models];
  int mcnt=0;
  double[] hypers=new double[nHyperParms()];
  for (int[] hidx=new int[nHyperParms()]; hidx != null; hidx=nextModel(hidx))   ms[mcnt++]=model(hypers(hidx,hypers));
  return ms;
}
