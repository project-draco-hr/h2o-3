{
  Model[] ms=new Model[_total_models];
  int mcnt=0;
  double[] hypers=new double[_hyperSearch.length];
  for (int[] hidx=new int[_hyperSearch.length]; hidx != null; hidx=nextModel(hidx))   ms[mcnt++]=model(hypers(hidx,hypers));
  return ms;
}
