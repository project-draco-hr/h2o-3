import unittest, random, sys, time
sys.path.extend(['.', '..', '../..', 'py'])
import h2o, h2o_browse as h2b, h2o_exec as h2e, h2o_import as h2i, h2o_cmd, h2o_util
from h2o_test import dump_json, verboseprint
exprList = ['(= !keys (ls))', '(= !x #1)']
ROWS = 100000
if (__name__ == '__main__'):
    h2o.unit_main()
