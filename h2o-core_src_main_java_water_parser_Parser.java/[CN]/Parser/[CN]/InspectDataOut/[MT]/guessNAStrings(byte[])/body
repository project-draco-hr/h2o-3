{
  String[] na_strings=new String[_ncols];
  for (int i=0; i < _ncols; ++i) {
    int nonemptyLines=_nlines - _nempty[i] - 1;
    if (types[i] == Vec.T_ENUM && _nzeros[i] > 0 && ((_nzeros[i] + _nstrings[i]) >= nonemptyLines) && (_domains[i].size() <= 0.95 * _nstrings[i])) {
      na_strings[i]="0";
    }
  }
  return na_strings;
}
