{
  nc.set_len(0);
  nc.set_len2(0);
  final int len=len();
  for (int i=0; i < len; i++) {
    int res=0xFF & _mem[i + _OFF];
    if (res == _NA)     nc.addNA();
 else     nc.addNum(res,0);
  }
  return nc;
}
