{
  Env captured=e.capture();
  captured=_pred.treeWalk(captured);
  double v=captured.popDbl();
  captured.popScope();
  if (v == 0)   if (_else == null)   return;
 else   _else.exec(e);
 else   super.exec(e);
}
