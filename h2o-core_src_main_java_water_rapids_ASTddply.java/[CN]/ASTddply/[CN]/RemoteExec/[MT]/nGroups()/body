{
  H2ONode[] array=H2O.CLOUD.members();
  Arrays.sort(array);
  int ngrps=_numgrps / array.length;
  if (Arrays.binarySearch(array,H2O.SELF) < _numgrps - ngrps * array.length)   ++ngrps;
  Log.info("Processing " + ngrps + " groups.");
  return ngrps;
}
