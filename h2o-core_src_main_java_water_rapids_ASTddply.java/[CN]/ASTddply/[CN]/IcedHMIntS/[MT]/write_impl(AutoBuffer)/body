{
  if (_m == null || _m.size() == 0)   return ab.put4(0);
 else {
    ab.put4(_m.size());
    for (    Integer g : _m.keySet()) {
      ab.put4(g);
      ab.putStr(_m.get(g));
    }
  }
  return ab;
}
