{
  int result=0;
  try {
    result=Integer.parseInt(a);
  }
 catch (  Exception e) {
    parseFailed("Argument " + _lastMatchedFor + " must be an integer (was given '"+ a+ "')");
  }
  return result;
}
