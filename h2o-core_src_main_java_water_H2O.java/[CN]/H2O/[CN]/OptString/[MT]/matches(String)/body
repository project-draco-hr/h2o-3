{
  boolean result=false;
  _lastMatchedFor=s;
  String candidate;
  candidate="-" + s;
  if (_s.equals(candidate)) {
    result=true;
  }
  candidate="--" + s;
  if (_s.equals(candidate)) {
    result=true;
  }
  return result;
}
