def signal_handler(self, signum, stackframe):
    if POLLING:
        h2o.connection().post(url_suffix=((u'Jobs/' + self.job_key) + u'/cancel'))
        print(u'Job {} was cancelled.'.format(self.job_key))
    else:
        signal.default_int_handler()
