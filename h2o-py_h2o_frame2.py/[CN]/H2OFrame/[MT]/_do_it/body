def _do_it(self, sb):
    if self._computed:
        sb += [(self._id + ' ')]
    elif (len(gc.get_referrers(self)) >= ExprNode.MAGIC_REF_COUNT):
        sb += self._eager(True)
    else:
        sb += self._eager(False)
