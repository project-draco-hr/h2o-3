{
  nc.set_len(len());
  nc.set_sparseLen(sparseLen());
  nc._ss=_strbuf;
  nc._sslen=_strbuf.length;
  nc._is=MemoryManager.malloc4(len());
  for (int i=0; i < len(); i++)   nc._is[i]=UnsafeUtils.get4(_mem,i << 2);
  return nc;
}
