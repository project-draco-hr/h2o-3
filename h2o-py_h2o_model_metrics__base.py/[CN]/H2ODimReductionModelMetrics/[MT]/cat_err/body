def cat_err(self):
    u'\n    :return: the Number of Misclassified categories over non-missing categorical entries, or None if not present.\n    '
    if MetricsBase._has(self._metric_json, u'caterr'):
        return self._metric_json[u'caterr']
    return None
