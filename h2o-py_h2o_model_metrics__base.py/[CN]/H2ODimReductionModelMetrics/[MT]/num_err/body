def num_err(self):
    u'\n    :return: the Sum of Squared Error over non-missing numeric entries, or None if not present.\n    '
    if MetricsBase._has(self._metric_json, u'numerr'):
        return self._metric_json[u'numerr']
    return None
