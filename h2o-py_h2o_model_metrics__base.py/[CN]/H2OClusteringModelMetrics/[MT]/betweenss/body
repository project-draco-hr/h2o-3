def betweenss(self):
    u'\n        :return: the Between Cluster Sum-of-Square Error, or None if not present.\n        '
    if MetricsBase._has(self._metric_json, u'betweenss'):
        return self._metric_json[u'betweenss']
    return None
