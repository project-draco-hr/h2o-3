{
  double d=chks[_split._col].atd(row);
  int bin;
  if (_split._equal == 0)   bin=d >= _splat ? 1 : 0;
 else   if (_split._equal == 1)   bin=d == _splat ? 1 : 0;
 else   bin=_split._bs.contains((int)d) ? 1 : 0;
  return _nids[bin];
}
