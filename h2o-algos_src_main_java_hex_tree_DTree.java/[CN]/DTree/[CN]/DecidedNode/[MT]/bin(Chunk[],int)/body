{
  float d=(float)chks[_split._col].atd(row);
  if (_split._equal == 0)   return d >= _splat ? 1 : 0;
 else   if (_split._equal == 1)   return d == _splat ? 1 : 0;
 else   return _split._bs.contains((int)d) ? 1 : 0;
}
