def local_cloud(test):
    test.a_node = h2o.H2O(use_this_ip_addr=test.cfg.cloud_topology()['local_cloud']['ip'], port=test.cfg.cloud_topology()['local_cloud']['port'])
    test.timeoutSecs = test.cfg.cloud_topology()['local_cloud']['timeoutSecs']
