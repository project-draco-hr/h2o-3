{
  _bins=new long[_nbins];
  for (int i=c.nextNZ(-1); i < c.len(); i=c.nextNZ(i)) {
    double d=c.at0(i);
    if (Double.isNaN(d))     continue;
    _bins[idx(d)]++;
  }
  if (c.isSparse())   _bins[idx(0.0)]+=(c.len() - c.sparseLen());
}
