{
  final Key rskey=vec.rollupStatsKey();
  Value val=DKV.get(rskey);
  if (val == null) {
    if (vec.length() > 0)     return null;
 else     return new RollupStats();
  }
 else {
    RollupStats rs=val.get(RollupStats.class);
    return rs.isReady() ? rs : null;
  }
}
