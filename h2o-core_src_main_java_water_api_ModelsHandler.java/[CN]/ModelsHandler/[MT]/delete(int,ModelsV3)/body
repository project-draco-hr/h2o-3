{
  Model model=getFromDKV(s.key.key());
  if (null == model)   throw new IllegalArgumentException("Model key not found: " + s.key);
  model.delete();
  InspectV1 i=new InspectV1();
  i.key=s.key;
  return s;
}
