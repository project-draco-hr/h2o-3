{
  Model model=getFromDKV(models.key);
  if (null == model)   throw new IllegalArgumentException("Model key not found: " + models.key);
  model.delete();
  InspectV1 s=new InspectV1();
  s.key=models.key;
  return s;
}
