{
  int len=ab.get4();
  _n=len;
  _nd=new NonBlockingHashSet[len];
  for (int i=0; i < len; ++i) {
    _nd[i]=new NonBlockingHashSet<>();
    int s=ab.get4();
    if (s == 0)     continue;
    for (int j=0; j < s; ++j)     _nd[i].add(ab.get8d());
  }
  return this;
}
