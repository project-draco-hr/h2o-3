{
  int len=_nd.length;
  ab.put4(len);
  for (  NonBlockingHashSet a_nd : _nd) {
    if (a_nd == null) {
      ab.put4(0);
      continue;
    }
    int s=a_nd.size();
    ab.put4(s);
    for (    Object d : a_nd)     ab.put8d((double)d);
  }
  return ab;
}
