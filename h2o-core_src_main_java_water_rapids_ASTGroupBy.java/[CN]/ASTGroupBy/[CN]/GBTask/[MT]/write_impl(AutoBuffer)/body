{
  if (_agg == null)   ab.put4(0);
 else {
    ab.put4(_agg.length);
    for (    AGG a : _agg)     ab.put(a);
  }
  ab.putA8(_gbCols);
  if (_g == null)   return ab.put4(0);
  ab.put4(_g.size());
  for (  G g : _g)   ab.put(g);
  return ab;
}
