{
  ab.putA8(_gbCols);
  ab.put4(_agg.length);
  for (int i=0; i < _agg.length; ++i)   ab.put(_agg[i]);
  if (_g == null)   return ab.put4(0);
  ab.put4(_g.size());
  for (  G g : _g)   ab.put(g);
  return ab;
}
