{
  int n=(int)((ASTNum)(E.parse()))._d;
  E.skipWS();
  _aggs=new AGG[n];
  for (int i=0; i < n; ++i) {
    String type=E.parseString(E.peekPlus());
    E.skipWS();
    int col=(int)((ASTNum)E.parse()).dbl();
    E.skipWS();
    String na=E.parseString(E.peekPlus());
    E.skipWS();
    String name=E.parseString(E.peekPlus());
    E.skipWS();
    _aggs[i]=new AGG(type,col,na,name);
  }
  return this;
}
