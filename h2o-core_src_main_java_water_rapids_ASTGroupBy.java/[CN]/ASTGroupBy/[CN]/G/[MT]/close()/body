{
  _avs=ArrayUtils.div(_sum.clone(),_N);
  for (int i=0; i < _vars.length; ++i) {
    _ND[i]=_nd[i] == null ? -1 : _nd[i].size();
    _nd[i]=null;
    _vars[i]=(_ss[i] - (_sum[i] * _sum[i]) / _N) / _N;
    _sdevs[i]=Math.sqrt(_vars[i]);
  }
}
