{
  int idx=prep.prep(fr);
  if (idx < 0) {
    drf._convert_to_enum=false;
    idx=~idx;
  }
  String rname=fr._names[idx];
  drf._response_column=fr.names()[idx];
  Vec resp=fr.remove(idx);
  fr.add(rname,resp);
  return fr.lastVec();
}
