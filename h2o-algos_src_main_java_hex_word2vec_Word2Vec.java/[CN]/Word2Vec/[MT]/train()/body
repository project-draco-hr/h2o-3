{
  if (_parms.sanityCheckParameters() > 0)   throw new IllegalArgumentException("Invalid parameters for Word2Vec: " + _parms.validationErrors());
  return start(new Word2VecDriver(),_parms._epochs);
}
