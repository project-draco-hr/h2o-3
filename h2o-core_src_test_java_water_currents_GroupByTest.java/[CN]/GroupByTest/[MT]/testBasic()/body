{
  Frame fr=null;
  String tree="(GB a.hex [1] [] mean 2 \"all\")";
  try {
    fr=checkTree(tree);
    throw H2O.unimpl();
  }
  finally {
    if (fr != null)     fr.delete();
    Keyed.remove(Key.make("a.hex"));
  }
}
