{
  StringBuilder sb=new StringBuilder();
  sb.append("Training time: " + PrettyPrint.msecs(run_time,true) + ". Processed "+ String.format("%,d",training_rows)+ " samples"+ " ("+ String.format("%.3f",epoch_counter)+ " epochs)."+ " Speed: "+ String.format("%.3f",1000. * training_rows / run_time)+ " samples/sec.\n");
  sb.append(model_info.toStringAll());
  sb.append(last_scored().toString());
  return sb.toString();
}
