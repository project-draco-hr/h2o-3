{
  ab.bodyHead();
  if (0 == http_status)   ab.title("H2O Error");
 else   ab.title(H2OError.httpStatusHeader(http_status));
  ab.p("<div class='alert alert-error'>").p(msg).p("</div>");
  if (null != stacktrace)   ab.p(Arrays.toString(stacktrace));
  return ab.bodyTail();
}
