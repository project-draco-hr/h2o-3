@property
def learn_rate_annealing(self):
    u'float: Scale the learning rate by this factor after each tree (e.g., 0.99 or 0.999)  (Default: 1.0)'
    return self._parms.get(u'learn_rate_annealing')
