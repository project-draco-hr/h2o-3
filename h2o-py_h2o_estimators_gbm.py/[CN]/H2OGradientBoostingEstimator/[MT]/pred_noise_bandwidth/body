@property
def pred_noise_bandwidth(self):
    u'\n        float: Bandwidth (sigma) of Gaussian multiplicative noise ~N(1,sigma) for tree node predictions (Default: 0.0)\n        '
    return self._parms.get(u'pred_noise_bandwidth')
