@property
def huber_alpha(self):
    u'\n        float: Desired quantile for Huber/M-regression (threshold between quadratic and linear loss, must be between 0\n        and 1). (Default: 0.9)\n        '
    return self._parms.get(u'huber_alpha')
