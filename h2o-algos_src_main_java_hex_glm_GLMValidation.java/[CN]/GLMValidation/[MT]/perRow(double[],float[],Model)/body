{
  _metricBuilder.perRow(ds,yact,m);
  if (!ArrayUtils.hasNaNsOrInfs(ds) && !ArrayUtils.hasNaNsOrInfs(yact)) {
    if (_glm._family == Family.binomial)     add2(yact[0],ds[2],1,0);
 else     add2(yact[0],ds[0],1,0);
  }
  return ds;
}
