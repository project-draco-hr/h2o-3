{
  if (_glm._family != Family.binomial)   throw new IllegalArgumentException("AUC only defined for family == 'binomial', got '" + _glm._family + "'");
  ModelMetricsBinomial metrics=(ModelMetricsBinomial)makeModelMetrics(m.clone(),f,Double.NaN);
  return metrics.auc()._auc;
}
