{
  if (_parms._family != Family.binomial)   throw new IllegalArgumentException("AUC only defined for family == 'binomial', got '" + _parms._family + "'");
  return ((MetricBuilderBinomial)_metricBuilder).auc();
}
