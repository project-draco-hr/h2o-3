{
  _wcount+=weight;
  ++_nobs;
  int c=(int)yreal;
  residual_deviance-=2 * weight * Math.log(ymodel[c + 1]);
  if (offset != 0)   null_deviance-=2 * weight * Math.log(offset + (_intercept ? Math.exp(_ymus[c]) : 0));
 else   null_deviance-=2 * weight * Math.log(_intercept ? _ymus[c] : 0);
}
