{
  _rank=rank;
  _ymu=ymu;
  _glm=glm;
  if (_glm.family == Family.binomial) {
    _cms=new ConfusionMatrix2[thresholds.length];
    for (int i=0; i < _cms.length; ++i)     _cms[i]=new ConfusionMatrix2(2);
  }
  this.dataKey=dataKey;
  this.thresholds=thresholds;
}
