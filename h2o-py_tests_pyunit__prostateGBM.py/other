import sys
import h2o
if (__name__ == '__main__'):
    args = sys.argv
    print args
    if (len(args) > 1):
        prostateGBM(args[1], int(args[2]))
    else:
        prostateGBM('localhost', 54321)
