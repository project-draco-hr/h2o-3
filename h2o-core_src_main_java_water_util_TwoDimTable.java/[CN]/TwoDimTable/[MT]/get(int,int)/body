{
  String cell=cellValues[row][col];
  if (cell.equals(""))   return null;
switch (colTypes[col]) {
case "double":
    return new Double(cell);
case "float":
  return new Float(cell);
case "integer":
return new Integer(cell);
case "long":
return new Long(cell);
default :
return cell;
}
}
