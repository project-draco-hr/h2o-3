{
  float sum=0;
  if (_nclass > 1) {
    for (int k=0; k < _nclass; k++)     sum+=(fs[k + 1]=(float)chk_tree(chks,k).atd(row));
  }
 else {
    sum+=(fs[0]=(float)chk_tree(chks,0).atd(row) / (float)chk_oobt(chks).atd(row));
    fs[1]=0;
  }
  return sum;
}
