{
  if (Job.class.isAssignableFrom(keyed_class))   return KeyV3.make(JobKeyV3.class,key);
 else   if (Frame.class.isAssignableFrom(keyed_class))   return KeyV3.make(FrameKeyV3.class,key);
 else   if (Model.class.isAssignableFrom(keyed_class))   return KeyV3.make(ModelKeyV3.class,key);
 else   if (Vec.class.isAssignableFrom(keyed_class))   return KeyV3.make(VecKeyV3.class,key);
 else   if (Grid.class.isAssignableFrom(keyed_class))   return KeyV3.make(GridKeyV3.class,key);
 else   return KeyV3.make(KeyV3.class,key);
}
