{
  while (true) {
    if (_x <= 0) {
      _x=0;
      break;
    }
    if (!(ppeek() == ' ' || ppeek() == ')' || ppeek() == ';')) {
      _x--;
      continue;
    }
    break;
  }
  return this;
}
