{
  int[] activeCols=ArrayUtils.removeSorted(_activeData.activeCols(),cols);
  if (_beta != null)   _beta=ArrayUtils.select(_beta,activeCols);
  if (_ginfo != null)   _ginfo._gradient=ArrayUtils.select(_ginfo._gradient,activeCols);
  _activeData=_activeData.filterExpandedColumns(activeCols);
  return activeCols;
}
