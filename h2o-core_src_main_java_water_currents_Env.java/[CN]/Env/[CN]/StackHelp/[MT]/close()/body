{
  Futures fs=null;
  int sp=sp();
  while (sp > _sp) {
    Frame fr=_refcnt.remove(--sp);
    for (    Vec vec : fr.vecs())     if (!inUse(vec)) {
      if (fs == null)       fs=new Futures();
      vec.remove(fs);
    }
  }
  if (fs != null)   fs.blockForPending();
}
