{
  Val v=_stack.remove(sp() - 1);
  if (v.isVec()) {
    Vec vec=((ValVec)v)._vec;
    if (subRef(vec) == 0) {
      if (_mayBeDead == null)       _mayBeDead=new ArrayList<>();
      _mayBeDead.add(vec);
    }
  }
  return v;
}
