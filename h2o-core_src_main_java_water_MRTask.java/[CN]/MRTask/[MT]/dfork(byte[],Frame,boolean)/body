{
  _topGlobal=true;
  _output_types=types;
  if (types != null && types.length > 0)   _vid=fr.anyVec().group().reserveKeys(types.length);
  _fr=fr;
  _nlo=selfidx();
  _nhi=(short)H2O.CLOUD.size();
  _run_local=run_local;
  setupLocal0();
  H2O.submitTask(this);
  return self();
}
