{
  double[] oneColumn=((DoubleColumnVector)oneDoubleColumn).vector;
  if (noNulls) {
switch (columnType) {
case "float":
      for (int rowIndex=0; rowIndex < rowNumber; rowIndex++)       dout.addNumCol(cIdx,(float)oneColumn[rowIndex]);
    break;
case "double":
  for (int rowIndex=0; rowIndex < rowNumber; rowIndex++)   dout.addNumCol(cIdx,oneColumn[rowIndex]);
}
}
 else {
for (int rowIndex=0; rowIndex < rowNumber; rowIndex++) {
if (isNull[rowIndex]) dout.addInvalidCol(cIdx);
 else {
switch (columnType) {
case "float":
    dout.addNumCol(cIdx,(float)oneColumn[rowIndex]);
  break;
case "double":
dout.addNumCol(cIdx,oneColumn[rowIndex]);
}
}
}
}
}
