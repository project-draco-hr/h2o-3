{
  long[] oneColumn=((LongColumnVector)oneLongColumn).vector;
  if (noNull) {
    for (int rowIndex=0; rowIndex < rowNumber; rowIndex++)     dout.addNumCol(cIdx,oneColumn[rowIndex],0);
  }
 else {
    for (int rowIndex=0; rowIndex < rowNumber; rowIndex++) {
      if (isNull[rowIndex])       dout.addInvalidCol(cIdx);
 else       dout.addNumCol(cIdx,oneColumn[rowIndex],0);
    }
  }
}
