{
  Vec w=f.vec(m._output.weightsName());
  Vec y=f.vec(m._output.responseName());
  if (w != null && y != null)   return new FrameUtils.WeightedSigma().doAll(y,w).weightedSigma();
  if (y != null)   return y.sigma();
  return Double.NaN;
}
