{
  BaseWalker<MP> hyperSpaceWalker;
  if (strategy == Strategy.Cartesian)   hyperSpaceWalker=new HyperSpaceWalker.CartesianWalker<>(params,hyperParams,paramsBuilderFactory);
 else   if (strategy == Strategy.Random)   hyperSpaceWalker=new HyperSpaceWalker.RandomDiscreteValueWalker<>(params,hyperParams,paramsBuilderFactory,max_models,max_time_ms,seed);
 else   throw new H2OIllegalArgumentException("strategy","GridSearch",strategy);
  return startGridSearch(destKey,hyperSpaceWalker);
}
