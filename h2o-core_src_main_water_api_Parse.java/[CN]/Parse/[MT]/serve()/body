{
  water.parser.ParseDataset2.parse(_hex,_srcs);
  System.out.println(_hex);
  for (  Key k : H2O.localKeySet())   System.out.println(k);
  return new Response("<a href=/2/DeepLearning.html?src=" + _hex + ">Parse done!</a>");
}
