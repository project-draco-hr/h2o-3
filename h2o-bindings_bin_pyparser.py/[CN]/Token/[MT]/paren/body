def paren(self):
    u"Return 1 for an OP '(' token, -1 for an OP ')' token, and 0 otherwise."
    if (self._op == OP):
        if (self._str == u'('):
            return 1
        if (self._str == u')'):
            return (-1)
    return 0
