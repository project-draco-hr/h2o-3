def indent(self):
    u'Return 1 for an INDENT token, -1 for a DEDENT token, and 0 otherwise.'
    if (self._op == INDENT):
        return 1
    if (self._op == DEDENT):
        return (-1)
    return 0
