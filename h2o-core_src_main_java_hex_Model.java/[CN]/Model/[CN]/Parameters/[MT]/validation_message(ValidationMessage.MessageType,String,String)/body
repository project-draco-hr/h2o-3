{
  if (null == _validation_messages)   _validation_messages=new ValidationMessage[1];
 else   _validation_messages=Arrays.copyOf(_validation_messages,_validation_messages.length + 1);
  _validation_messages[_validation_messages.length - 1]=new ValidationMessage(message_type,field_name,message);
}
