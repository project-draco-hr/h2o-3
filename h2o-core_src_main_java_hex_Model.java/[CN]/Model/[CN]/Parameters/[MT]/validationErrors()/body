{
  StringBuilder sb=new StringBuilder();
  for (  ValidationMessage vm : _validation_messages)   if (vm.message_type == ValidationMessage.MessageType.ERROR)   sb.append((sb.length() > 0 ? "" : "; ")).append(vm.toString());
  return sb.toString();
}
