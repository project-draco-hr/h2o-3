{
  DKV.put(mm);
  final Key k=mm._key;
  Replacer r=new Replacer(k);
  r.invoke(_key);
  Key[] res=r._res;
  if (res == null)   incrementModelMetrics(_output,k);
 else   _output._model_metrics=res;
  return mm;
}
