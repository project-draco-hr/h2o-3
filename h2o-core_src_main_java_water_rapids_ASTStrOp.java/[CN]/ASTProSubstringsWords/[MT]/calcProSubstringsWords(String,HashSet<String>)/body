{
  int wordCount=0;
  int N=str.length();
  if (N < 2)   return 0;
  double totalSubstrings=N * (N - 1) / 2;
  for (int i=0; i < N - 1; i++)   for (int j=i + 2; j < N + 1; j++) {
    if (words.contains(str.substring(i,j)))     wordCount+=1;
  }
  return wordCount / totalSubstrings;
}
