{
  final Chunk resp=chk_resp(chks);
  final Chunk offset=hasOffsetCol() ? chk_offset(chks) : new C0DChunk(0,chks[0]._len);
  final Chunk preds=chk_tree(chks,0);
  for (int i=0; i < chks[0].len(); ++i) {
    nc[0].addNum(resp.atd(i) - (preds.atd(i) + offset.atd(i)));
  }
}
