{
  if (Double.isNaN(objNew))   return true;
  double f_hat=0;
  for (int i=0; i < betaNew.length; ++i)   f_hat+=gradOld[i] * (betaNew[i] - betaOld[i]);
  f_hat=c1 * step * f_hat + objOld;
  return objNew < f_hat;
}
