{
  for (int i=0; i < objVals.length; ++i) {
    if (!needLineSearch(step,ginfo._objVal,objVals[i],pk,ginfo._gradient))     return new LineSearchSol(objVals[i],step,i);
    step*=stepDec;
  }
  return null;
}
