{
  Frame fr=stk.track(asts[1].exec(env)).getFrame();
  String res="FALSE";
  for (int i=0; i < fr.vecs().length; ++i)   if (fr.vecs()[i].isCategorical()) {
    res="TRUE";
    break;
  }
  return new ValStr(res);
}
