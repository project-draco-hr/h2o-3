def enums():
    '\n    Return the dictionary of H\xe2\x82\x82O enums, retrieved from data in schemas(). For each entry in the dictionary its key is\n    the name of the enum, and the value is the set of all enum values.\n    '
    enumset = defaultdict(set)
    for schema in schemas():
        for field in schema['fields']:
            if (field['type'] == 'enum'):
                enumset[field['schema_name']].update(field['values'])
    return enumset
