@property
def default_params(self):
    u'\n        Get default parameters of a model\n\n        :return: A dictionary of default parameters for the model\n        '
    params = {}
    for p in self.parms:
        params[p] = self.parms[p][u'default_value']
    return params
