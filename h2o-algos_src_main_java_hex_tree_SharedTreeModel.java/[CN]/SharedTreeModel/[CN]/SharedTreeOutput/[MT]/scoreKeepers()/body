{
  List<ScoreKeeper> sk=new ArrayList<>();
  ScoreKeeper[] ska=_validation_metrics != null ? _scored_valid : _scored_train;
  for (int i=0; i < ska.length; ++i) {
    if (!ska[i].isEmpty())     sk.add(ska[i]);
  }
  return sk.toArray(new ScoreKeeper[0]);
}
