{
  super("Key '" + name.toString() + "' not found for argument: "+ argument,"Key '" + name.toString() + "' not found for argument: "+ argument);
  this.values=new IcedHashMap<>();
  this.values.put("argument",argument);
  this.values.put("name",name);
}
