{
  init();
  return new Quantiles(source_key.vecs()[source_key.find(column)],source_key,quantile,max_qbins,multiple_pass,interpolation_type,max_ncols,column_name,quantile_requested,interpolation_type_used,interpolated,iterations,result,result_single);
}
