{
  if (isOp(node)) {
    JsonObject jo=node.get("astop").getAsJsonObject();
    if (jo.get("type").getAsString().equals("BinaryOperator")) {
      if (Arrays.asList(COMPAREOPS).contains(jo.get("operator").getAsString()))       return true;
    }
  }
  return false;
}
