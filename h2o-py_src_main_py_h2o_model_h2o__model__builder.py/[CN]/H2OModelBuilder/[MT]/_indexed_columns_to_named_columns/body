@staticmethod
def _indexed_columns_to_named_columns(x, y, dataset):
    if isinstance(x[0], int):
        x = [dataset.names()[i] for i in x]
    if isinstance(y, int):
        y = dataset.names()[y]
    return (x, y)
