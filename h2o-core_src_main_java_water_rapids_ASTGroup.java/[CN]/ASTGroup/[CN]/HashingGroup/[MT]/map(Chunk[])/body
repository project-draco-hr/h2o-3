{
  if (_kill._killed)   return;
  IcedHashMap<GKX,String> gs=_gs;
  GKX gOld, gWork=_gTemplate.clone();
  final int rlen=cs[0]._len;
  for (int row=0; row < rlen; row++) {
    gWork.fill(cs,row,0);
    if (gs.putIfAbsent(gWork,"") == null)     gWork=_gTemplate.clone();
  }
}
