{
  GK0[] gkxs=gs.keySet().toArray(new GK0[gs.size()]);
  Arrays.sort(gkxs,new java.util.Comparator<GK0>(){
    @Override public int compare(    GK0 g1,    GK0 g2){
      return g1.compare(g2);
    }
    @Override public boolean equals(    Object o){
      throw H2O.unimpl();
    }
  }
);
  for (int gnum=0; gnum < gkxs.length; gnum++)   gkxs[gnum]._gnum=gnum;
  return gkxs;
}
