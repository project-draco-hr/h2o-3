{
  isAvailabel=true;
  if (StringUtils.isEmpty(dataSetId) || StringUtils.isEmpty(dataSetDirectory) || StringUtils.isEmpty(fileName)|| StringUtils.isEmpty(responseColumn)) {
    isAvailabel=false;
  }
 else   if (columnNames == null || columnNames.length == 0) {
    isAvailabel=false;
  }
 else   if (columnTypes == null || columnTypes.length == 0) {
    isAvailabel=false;
  }
}
