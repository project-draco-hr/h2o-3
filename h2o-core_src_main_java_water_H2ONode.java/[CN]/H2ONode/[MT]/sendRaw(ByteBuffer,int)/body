{
  _msgQ.put(new UDPMessage(Arrays.copyOf(bb.array(),bb.limit()),-priority));
  if (_sendThread == null) synchronized (this) {
    if (_sendThread == null)     (_sendThread=new UDP_TCP_SendThread()).start();
  }
}
