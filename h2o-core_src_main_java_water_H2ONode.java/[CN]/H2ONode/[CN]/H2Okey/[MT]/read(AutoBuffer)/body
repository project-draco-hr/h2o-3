{
  try {
    int size=ab.get1() == 1 ? 4 : 16;
    InetAddress inet=InetAddress.getByAddress(ab.getA1(size));
    int port=ab.get2();
    return new H2Okey(inet,port);
  }
 catch (  UnknownHostException e) {
    throw Log.throwErr(e);
  }
}
