{
  try {
    Scope.enter();
    _parms.read_lock_frames(DeepLearning.this);
    init(true);
    buildModel();
  }
 catch (  Throwable t) {
    t.printStackTrace();
    cancel2(t);
    throw t;
  }
 finally {
    _parms.read_unlock_frames(DeepLearning.this);
    Scope.exit();
    done();
  }
  tryComplete();
}
