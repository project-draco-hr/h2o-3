{
  if (null != i._val) {
    key=KeySchema.make(i._val._key);
    if (i._val.isFrame())     kind="frame";
 else     if (i._val.isModel())     kind="model";
 else     if (i._val.isVec())     kind="vec";
 else     if (i._val.isKey())     kind="key";
 else     kind="unknown";
  }
  schema=i._schema;
  if (null != schema)   schema.fillFromImpl(null == i._val ? null : i._val.get());
  return this;
}
