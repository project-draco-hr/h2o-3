{
  try {
    ForkJoinPool.managedBlock(this);
  }
 catch (  InterruptedException ignore) {
  }
catch (  RuntimeException re) {
    if (!hasException())     setException(re);
  }
  DException.DistributedException de=getDException();
  if (de != null)   throw de;
  return self();
}
