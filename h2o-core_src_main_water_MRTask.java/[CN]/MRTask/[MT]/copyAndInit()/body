{
  T x=(T)clone();
  System.out.println("MRTask.copyAndInit: " + x.getCompleter());
  x.setCompleter(this);
  x._topLocal=false;
  x._nleft=x._nrite=null;
  x._left=x._rite=null;
  x._fs=_fs;
  x._profile=null;
  x.setPendingCount(0);
  return x;
}
