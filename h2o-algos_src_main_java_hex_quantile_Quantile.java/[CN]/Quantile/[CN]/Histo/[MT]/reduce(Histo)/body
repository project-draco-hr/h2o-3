{
  for (int i=0; i < _nbins; i++)   if (_bins[i] == 0)   _elems[i]=h._elems[i];
 else   if (h._bins[i] > 0 && _elems[i] != h._elems[i])   _elems[i]=Double.NaN;
  ArrayUtils.add(_bins,h._bins);
  if (_hs != null)   throw H2O.unimpl();
}
