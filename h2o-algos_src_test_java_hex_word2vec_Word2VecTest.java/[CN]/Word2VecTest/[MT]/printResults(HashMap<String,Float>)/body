{
  TreeMap<Float,String> reversedMap=new TreeMap<>();
  for (  Map.Entry entry : hm.entrySet())   reversedMap.put((Float)entry.getValue(),(String)entry.getKey());
  for (  Map.Entry entry : reversedMap.descendingMap().entrySet())   Log.info(entry.getKey() + ", " + entry.getValue());
}
