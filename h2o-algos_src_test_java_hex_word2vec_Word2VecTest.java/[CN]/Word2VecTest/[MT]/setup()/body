{
  File f=new File(testFName);
  if (!f.exists())   throw new RuntimeException("File " + testFName + " not found.  Please run ./gradlew syncBigdataLaptop (or gradlew.bat syncBigdataLaptop for Windows) to retrieve the file.\"");
  stall_till_cloudsize(1);
}
