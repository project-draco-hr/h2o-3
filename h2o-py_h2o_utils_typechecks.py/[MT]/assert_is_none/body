def assert_is_none(s, reason=None):
    u'Assert that the argument is None.'
    if (s is not None):
        raise H2OValueError((u'`%s` should be None %s' % (_get_variable_name(), (reason or u''))))
