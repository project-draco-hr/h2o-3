def __str__(self):
    res = [_get_type_name(tt) for tt in self._types]
    if ((len(res) == 2) and (u'None' in res)):
        res.remove(u'None')
        return (u'?' + res[0])
    else:
        return u'|'.join(res)
