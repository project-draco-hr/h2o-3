def __str__(self):
    if (len(self._types) > 1):
        return (u'!(%s)' % str(u'|'.join((_get_type_name(tt) for tt in self._types))))
    else:
        return (u'!' + _get_type_name(self._types[0]))
