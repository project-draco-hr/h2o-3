{
  XlsParser p=new XlsParser(new ParseSetup(true,0,0,null,ParserType.XLS,ParseSetup.GUESS_SEP,false,ParseSetup.GUESS_HEADER,ParseSetup.GUESS_COL_CNT,null,null,null,null,null));
  p._buf=bytes;
  p._lim=bytes.length;
  InspectDataOut dout=new InspectDataOut();
  try {
    p.streamParse(new ByteArrayInputStream(bytes),dout);
  }
 catch (  IOException e) {
    throw new RuntimeException(e);
  }
  return new ParseSetup(dout._ncols > 0 && dout._nlines > 0 && dout._nlines > dout._invalidLines,dout._invalidLines,0,dout.errors(),ParserType.XLS,ParseSetup.GUESS_SEP,false,dout.colNames() == null ? ParseSetup.NO_HEADER : ParseSetup.HAS_HEADER,dout._ncols,dout.colNames(),dout.guessTypes(),null,null,dout._data);
}
