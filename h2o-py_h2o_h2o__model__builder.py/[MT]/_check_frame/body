def _check_frame(x, y, response):
    if (x is None):
        return None
    x._eager()
    if (y is not None):
        y._eager()
        response._eager()
        x[response._col_names[0]] = y
    return x
