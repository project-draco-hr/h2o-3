def _check_extra_col(x, vx, colname, kwargs):
    x = _check_frame(x, kwargs[colname], kwargs[colname])
    if (vx is not None):
        vx = _check_frame(vx, kwargs[colname], kwargs[colname])
    kwargs[colname] = kwargs[colname]._col_names[0]
    return (x, vx)
