def _add_weights_col(target, source, weights_column):
    if (not isinstance(weights_column, str)):
        raise ValueError('`weights_column` must be a column name, but got {0}'.format(weights_column))
    target[weights_column] = source[weights_column]
