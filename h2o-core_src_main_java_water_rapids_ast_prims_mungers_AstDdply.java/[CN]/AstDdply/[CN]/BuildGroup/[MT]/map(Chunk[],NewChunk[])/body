{
  AstGroup.G gWork=new AstGroup.G(_gbCols.length,null);
  for (int row=0; row < cs[0]._len; row++) {
    gWork.fill(row,cs,_gbCols);
    int gnum=(int)_gss.getk(gWork)._dss[0][0];
    ncs[gnum].addNum(row);
  }
}
