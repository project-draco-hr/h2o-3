{
  if (mults == null)   return clusters;
  double[][] value=new double[clusters.length][clusters[0].length];
  for (int row=0; row < clusters.length; row++)   for (int col=0; col < clusters[row].length; col++)   value[row][col]=clusters[row][col] * mults[col] + means[col];
  return value;
}
