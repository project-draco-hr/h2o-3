{
  int K=cats.length;
  int ncats=cats[0].length;
  for (int clu=0; clu < K; clu++)   for (int col=0; col < ncats; col++)   clusters[clu][col]=ArrayUtils.maxIndex(cats[clu][col]);
  return clusters;
}
