{
  if (modes[i] == -1) {
    if (Double.isNaN(d))     d=means[i];
    if (mults != null) {
      d-=means[i];
      d*=mults[i];
    }
  }
 else {
    if (Double.isNaN(d))     d=modes[i];
  }
  return d;
}
