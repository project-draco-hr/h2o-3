{
  boolean any=false;
  for (int i=0; i < f.vecs().length; i++) {
    if (filter(f.vecs()[i])) {
      if (any)       msg+=", ";
      any=true;
      msg+=f._names[i];
    }
  }
  if (any) {
    error("_train",msg);
    if (expensive)     Log.info(msg);
  }
}
