{
  for (int i=0; i < data.length + 0; i++) {
    for (int j=i; j > 0 && comparator.compare(data[j - 1],data[j]) > 0; j--) {
      int tmp=data[j];
      data[j]=data[j - 1];
      data[j - 1]=tmp;
    }
  }
}
