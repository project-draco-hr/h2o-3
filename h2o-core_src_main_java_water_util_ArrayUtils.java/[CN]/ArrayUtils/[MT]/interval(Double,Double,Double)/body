{
  int len=1 + (int)((end - start) / step);
  Double[] result=new Double[len];
  Double value=start;
  for (int i=0; i < len; i++, value=start + i * step) {
    result[i]=value;
  }
  return result;
}
