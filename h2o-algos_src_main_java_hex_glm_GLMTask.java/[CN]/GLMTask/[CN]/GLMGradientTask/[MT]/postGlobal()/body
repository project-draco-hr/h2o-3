{
  if (_validate) {
    _val.computeAIC();
  }
  for (int j=0; j < _beta.length - (_dinfo._intercept ? 1 : 0); ++j)   _gradient[j]=_gradient[j] * _reg + _currentLambda * _beta[j];
  if (_dinfo._intercept)   _gradient[_gradient.length - 1]*=_reg;
}
