{
  for (int i=0; i < _objVals.length; ++i) {
    _objVals[i]+=.5 * _currentLambda * ArrayUtils.l2norm2(_beta[i],_dinfo._intercept);
    for (int j=0; j < _beta[i].length - (_dinfo._intercept ? 1 : 0); ++j)     _gradient[i][j]+=_currentLambda * _beta[i][j];
  }
}
