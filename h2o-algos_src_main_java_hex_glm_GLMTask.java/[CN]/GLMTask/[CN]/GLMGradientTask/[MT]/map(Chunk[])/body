{
  _gradient=MemoryManager.malloc8d(_beta.length);
  if (chks.length >= 100 || mostlySparse(chks))   goByCols(chks);
 else   goByRows(chks);
  _objVal*=_reg;
  double[] g=_gradient;
  for (int j=0; j < g.length; ++j)   g[j]*=_reg;
}
