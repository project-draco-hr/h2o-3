{
  ArrayUtils.mult(_gradient,_reg);
  int P=(int)_beta[0].length;
  for (int c=0; c < _beta.length; ++c)   for (int j=0; j < _beta[c].length - (_dinfo._intercept ? 1 : 0); ++j)   _gradient[c * P + j]+=_currentLambda * _beta[c][j];
}
