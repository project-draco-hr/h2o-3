{
  mu=_parms.linkInv(eta + o);
  likelihood=w * _parms.likelihood(y,mu);
  double var=Math.max(1e-6,_parms.variance(mu));
  double d=_parms.linkDeriv(mu);
  z=eta + (y - mu) * d;
  this.w=w / (var * d * d);
}
