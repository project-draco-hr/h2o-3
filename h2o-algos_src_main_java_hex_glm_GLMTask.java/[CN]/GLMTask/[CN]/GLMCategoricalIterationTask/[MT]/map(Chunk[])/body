{
  Chunk x=chks[0];
  Chunk o=chks[1];
  Chunk w=chks[2];
  Chunk y=chks[3];
  for (int i=0; i < x._len; ++i) {
    if (x.isNA(i))     continue;
    int c=(int)x.at8(i);
    double eta=_beta[c];
    double off=o.atd(i) - eta;
  }
}
