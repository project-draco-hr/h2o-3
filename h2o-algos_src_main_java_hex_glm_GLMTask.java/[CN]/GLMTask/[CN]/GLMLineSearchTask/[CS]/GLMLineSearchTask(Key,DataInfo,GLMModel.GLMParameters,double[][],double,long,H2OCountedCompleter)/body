{
  super(jobKey,dinfo,glm,cmp);
  _glmts=new GLMIterationTask[betas.length];
  for (int i=0; i < _glmts.length; ++i)   _glmts[i]=new GLMIterationTask(jobKey,dinfo,glm,false,true,true,betas[i],ymu,1.0 / nobs,new float[]{0},null);
}
