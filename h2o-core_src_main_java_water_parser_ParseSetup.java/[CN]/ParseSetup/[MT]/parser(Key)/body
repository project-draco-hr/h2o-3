{
switch (_parse_type) {
case CSV:
    return new CsvParser(this,jobKey);
case XLS:
  return new XlsParser(this,jobKey);
case SVMLight:
return new SVMLightParser(this,jobKey);
case ARFF:
return new ARFFParser(this,jobKey);
case AVRO:
return new AvroParser(this,jobKey,((AvroParser.AvroParseSetup)this).header);
}
throw new H2OIllegalArgumentException("Unknown file type.  Parse cannot be completed.","Attempted to invoke a parser for ParseType:" + _parse_type + ", which doesn't exist.");
}
