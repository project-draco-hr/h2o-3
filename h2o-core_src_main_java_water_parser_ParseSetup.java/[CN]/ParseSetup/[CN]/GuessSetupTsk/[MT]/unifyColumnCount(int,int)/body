{
  if (cntA == cntB)   return cntA;
 else   if (cntA == 0)   return cntB;
 else   if (cntB == 0)   return cntA;
 else {
    throw new ParseDataset.H2OParseException("Files conflict in number of columns. " + cntA + " vs. "+ cntB+ ".");
  }
}
