{
  if (chkHdrA == GUESS_HEADER || chkHdrB == GUESS_HEADER)   throw new H2OParseException("Unable to determine header on a file. Not expected.");
  if (chkHdrA == HAS_HEADER || chkHdrB == HAS_HEADER)   return HAS_HEADER;
 else   return NO_HEADER;
}
