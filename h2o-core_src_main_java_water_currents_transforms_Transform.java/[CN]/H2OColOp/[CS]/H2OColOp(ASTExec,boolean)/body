{
  _ast=ast;
  _inplace=inplace;
  _params=new HashMap<String,Object>();
  _fun=_ast.str();
  String[] args=_ast.getArgs();
  if (args != null && args.length > 1) {
    for (int i=2; i < args.length; ++i)     _params.put(args[i],_ast._asts[i + 1]);
  }
}
