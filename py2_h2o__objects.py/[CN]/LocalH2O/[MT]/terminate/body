def terminate(self):
    if (1 == 0):
        self.shutdown_all()
    if self.is_alive():
        print ("\nShutdown didn't work fast enough for local node? : %s. Will kill though" % self)
    self.terminate_self_only()
