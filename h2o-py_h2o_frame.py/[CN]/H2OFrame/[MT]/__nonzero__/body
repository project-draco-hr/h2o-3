def __nonzero__(self):
    if ((self.nrow > 1) or (self.ncol > 1)):
        raise ValueError(u'This operation is not supported on an H2OFrame. Try using parantheses. Did you mean & (logical and), | (logical or), or ~ (logical not)?')
    else:
        return self.__len__()
