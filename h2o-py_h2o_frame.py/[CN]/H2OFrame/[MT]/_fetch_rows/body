def _fetch_rows(self, off, nrows):
    if self._data:
        if ((off == 0) and ((len(self._data) - 1) >= nrows)):
            return self._data[:(nrows + 1)]
    print off
    print nrows
    print self._data
    raise ValueError('unimpl')
