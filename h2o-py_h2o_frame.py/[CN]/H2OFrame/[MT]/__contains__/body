def __contains__(self, i):
    if (_is_str_list(i) or isinstance(i, (unicode, str))):
        return H2OFrame(expr=ExprNode('h2o.which', self, i)).any()
    else:
        return (all([any((t == self)) for t in i]) if _is_num_list(i) else any((i == self)))
