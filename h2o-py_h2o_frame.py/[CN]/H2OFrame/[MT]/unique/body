def unique(self):
    u'Extract the unique values in the column.\n\n        Returns\n        -------\n          H2OFrame of just the unique values in the column.\n        '
    return H2OFrame._expr(expr=ExprNode(u'unique', self))
