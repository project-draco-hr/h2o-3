def _eager(self, pytmp=True):
    if (not self._computed):
        sb = self._ast._eager()
        if pytmp:
            h2o.rapids(ExprNode._collapse_sb(sb), self._id)
            sb = ['%', self._id, ' ']
            self._update()
        return sb
