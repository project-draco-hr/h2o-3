def __delitem__(self, i):
    if isinstance(i, str):
        for v in self._vecs:
            if (i == v.name()):
                self._vecs.remove(v)
                return
            raise KeyError((('Name ' + i) + ' not in Frames'))
        raise NotImplementedError
