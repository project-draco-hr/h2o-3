def nlevels(self):
    u'Get the number of factor levels for this frame.\n\n        Returns\n        -------\n          A list of the number of levels per column.\n        '
    levels = self.levels()
    return ([len(l) for l in levels] if levels else 0)
