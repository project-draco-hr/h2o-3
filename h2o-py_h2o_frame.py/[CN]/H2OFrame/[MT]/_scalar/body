def _scalar(self):
    res = self.as_data_frame(False)
    if (res[1] == []):
        return float('nan')
    res = res[1][0]
    if (res == 'TRUE'):
        return True
    if (res == 'FALSE'):
        return False
    try:
        return float(res)
    except:
        return res
