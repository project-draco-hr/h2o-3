@property
def names(self):
    u'The list of column names, one name per H2OVec.'
    if (not self._ex._cache.names_valid()):
        self._ex._cache.flush()
        self._frame(True)
    return self._ex._cache.names
