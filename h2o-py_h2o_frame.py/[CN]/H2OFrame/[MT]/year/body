def year(self):
    u'\n    Returns\n    -------\n      Year column from a msec-since-Epoch column\n    '
    fr = H2OFrame._expr(expr=ExprNode(u'year', self), cache=self._ex._cache)
    if fr._ex._cache.types_valid():
        fr._ex._cache.types = {k: u'int' for k in self._ex._cache.types.keys()}
    return fr
