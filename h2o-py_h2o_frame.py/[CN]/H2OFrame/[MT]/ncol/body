@property
def ncol(self):
    u'The number of columns in the H2OFrame.'
    if (not self._ex._cache.ncols_valid()):
        self._ex._cache.flush()
        self._frame(True)
    return self._ex._cache.ncols
