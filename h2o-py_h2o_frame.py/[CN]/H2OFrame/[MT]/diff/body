def diff(self):
    u'Computes the lag1 diff on a numeric column.\n\n        Returns\n        -------\n          The lag1 difference for a numeric column (expects operation to occur over H2OFrame\n          of a single column).\n        '
    return H2OFrame._expr(expr=ExprNode(u'difflag1', self))
