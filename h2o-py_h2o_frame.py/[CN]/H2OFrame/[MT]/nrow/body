@property
def nrow(self):
    u'The number of rows in the H2OFrame.'
    if (not self._ex._cache.nrows_valid()):
        self._ex._cache.flush()
        self._frame(True)
    return self._ex._cache.nrows
