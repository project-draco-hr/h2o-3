def asfactor(self):
    '\n    :return:\n    '
    expr = (("(as.factor '" + self.key()) + "')")
    res = h2o.rapids(expr)
    return H2OVec(self._name, Expr(op=res['vec_ids'][0]['name'], length=res['num_rows']))
