@property
def regularization_x(self):
    u'\n        Enum["none", "quadratic", "l2", "l1", "non_negative", "one_sparse", "unit_one_sparse", "simplex"]:\n        Regularization function for X matrix (Default: "none")\n        '
    return self._parms.get(u'regularization_x')
