@property
def loss_by_col(self):
    u'\n        List[Enum["quadratic", "absolute", "huber", "poisson", "hinge", "logistic", "periodic", "categorical",\n        "ordinal"]]: Loss function by column (override)\n        '
    return self._parms.get(u'loss_by_col')
