def __init__(self, frame, noRefCnt=False):
    super(KeyInit, self).__init__()
    assert isinstance(frame, basestring)
    self.frame = frame
    if (not noRefCnt):
        self.refcntInc()
        self.assignIfRoot()
