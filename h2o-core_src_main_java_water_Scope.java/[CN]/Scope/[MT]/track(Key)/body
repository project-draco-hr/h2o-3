{
  if (k.user_allowed() || !k.isVec())   return;
  Scope scope=_scope.get();
  if (scope == null)   return;
  if (scope._keys.size() == 0)   return;
  if (!scope._keys.peek().contains(k))   scope._keys.peek().add(k);
}
