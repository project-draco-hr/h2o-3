@staticmethod
def _calc_base_url(url_suffix=None):
    url_base = 'http://{}:{}/{}/{}'
    if (not url_suffix):
        raise ValueError('No url suffix supplied.')
    url_suffix += '.json'
    return url_base.format(H2OConnection.ip(), H2OConnection.port(), H2OConnection.rest_version(), url_suffix)
