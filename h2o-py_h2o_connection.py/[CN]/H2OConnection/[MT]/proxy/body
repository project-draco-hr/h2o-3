@property
def proxy(self):
    u'URL of the proxy server used for the connection (or None if there is no proxy).'
    if (self._proxies is None):
        return None
    return self._proxies.values()[0]
