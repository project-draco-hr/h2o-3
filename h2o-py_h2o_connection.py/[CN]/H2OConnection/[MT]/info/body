def info(self, refresh=False):
    if (self._stage == 0):
        return None
    if refresh:
        self._cluster_info = self.api(u'GET /3/Cloud')
    self._cluster_info.connection = self
    return self._cluster_info
