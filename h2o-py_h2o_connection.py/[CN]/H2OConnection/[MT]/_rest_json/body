def _rest_json(self, url_suffix, params, method, file_upload_info, **kwargs):
    raw_txt = self._do_raw_rest(url_suffix, params, method, file_upload_info, **kwargs)
    return H2OConnection._from_json(raw_txt, **kwargs)
