{
  ArrayList<Integer> w=new ArrayList<>();
  for (int i=0; i < in.length; ++i)   if (in[i] == 1)   w.add(i + _one_based);
  out=new double[w.size()];
  for (int i=0; i < w.size(); ++i)   out[i]=w.get(i);
  return out;
}
