{
  Frame ary=env.peekAry();
  Vec[] nvecs=new Vec[ary.numCols()];
  for (int c=0; c < ary.numCols(); ++c)   (nvecs[c]=ary.vecs()[c]).setDomain(null);
  ary=new Frame(ary._names,nvecs);
  env.poppush(1,new ValFrame(ary));
}
