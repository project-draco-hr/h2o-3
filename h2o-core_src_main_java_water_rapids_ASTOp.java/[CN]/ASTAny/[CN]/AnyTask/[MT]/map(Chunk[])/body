{
  int j=0;
  for (  Chunk aC : c) {
    for (j=0; j < c[0]._len; ++j) {
      if (!any) {
        if (aC.isNA(j) && !_narm) {
          any=false;
          break;
        }
        any|=aC.atd(j) == 1;
      }
 else       break;
    }
    if (j != c[0]._len)     break;
  }
}
