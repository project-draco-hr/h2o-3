{
  AST arg=E.parse();
  AST a=E.parse();
  if (a instanceof ASTId)   _narm=((ASTNum)E._env.lookup((ASTId)a))._d == 1;
  E.eatEnd();
  ASTAny res=(ASTAny)clone();
  res._asts=new AST[]{arg};
  return res;
}
