{
  double n=((ASTNum)(E.parse()))._d;
  for (int i=0; i < n; ++i) {
    E.skipWS();
    String acc=E.parseString(E.peekPlus());
    E.skipWS();
    O o=(O)E.parse();
    _ops.put(acc,o);
  }
  return (ROp)clone();
}
