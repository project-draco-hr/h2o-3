{
  _red=ab.get(ROp.class);
  int len=ab.get4();
  if (len == 0)   return this;
  _accum=new NonBlockingHashMap<>();
  for (int i=0; i < len; ++i)   _accum.put(ab.getStr(),ab.get(Val.class));
  return this;
}
