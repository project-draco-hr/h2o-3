{
  Frame f=e.popAry();
  if (f.numCols() != 1)   throw new IllegalArgumentException("Expected one column, got " + f.numCols());
  NonBlockingHashMap<String,Val> accum=new RTask(_red).doAll(f.anyVec())._accum;
  e.push(_combine.combine(accum));
}
