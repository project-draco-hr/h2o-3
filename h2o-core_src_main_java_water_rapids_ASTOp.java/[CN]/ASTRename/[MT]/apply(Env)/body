{
  Frame fr=e.popAry();
  if (_deepCopy) {
    if (fr._key != null)     DKV.remove(fr._key);
    Frame fr2=new Frame(Key.make(_newname),fr.names(),fr.vecs());
    DKV.put(fr2._key,fr2);
  }
 else {
    Frame fr2=fr.deepCopy(_newname);
    DKV.put(fr2._key,fr2);
    e.pushAry(fr2);
  }
}
