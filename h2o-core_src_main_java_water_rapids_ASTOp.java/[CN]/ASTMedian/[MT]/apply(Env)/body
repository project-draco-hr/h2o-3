{
  Frame fr=env.popAry();
  Key tk=null;
  if (fr._key == null) {
    DKV.put(tk=Key.make(),fr=new Frame(tk,fr.names(),fr.vecs()));
  }
  double median=median(fr,QuantileModel.CombineMethod.INTERPOLATE);
  if (tk != null) {
    DKV.remove(tk);
  }
  env.push(new ValNum(median));
}
