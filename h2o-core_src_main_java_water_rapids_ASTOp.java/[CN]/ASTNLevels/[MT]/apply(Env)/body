{
  int nlevels=0;
  Frame fr=env.popAry();
  if (fr.numCols() != 1)   nlevels=0;
 else {
    Vec v=fr.anyVec();
    nlevels=v.isEnum() ? v.domain().length : 0;
  }
  env.push(new ValNum(nlevels));
}
