{
  for (int i=0; i < cs[0]._len; ++i) {
    int h=_m.get(mix(cs[0].at8(i),cs[1].at8(i)));
    long offset=ssid(h);
    long c=_cnts[h];
    while (!_unsafe.compareAndSwapLong(_cnts,offset,c,c + 1))     c=_cnts[h];
  }
}
