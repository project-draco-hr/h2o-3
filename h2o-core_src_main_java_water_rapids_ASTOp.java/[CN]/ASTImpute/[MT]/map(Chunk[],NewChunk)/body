{
  ASTGroupBy.G g=new ASTGroupBy.G(cols.length);
  double impute_value;
  Chunk ch=c[colIdx];
  for (int i=0; i < c[0]._len; ++i) {
    g.fill(i,c,cols);
    impute_value=_s.get(g)._avs[0];
    n.addNum(ch.isNA(i) ? impute_value : ch.atd(i));
  }
}
