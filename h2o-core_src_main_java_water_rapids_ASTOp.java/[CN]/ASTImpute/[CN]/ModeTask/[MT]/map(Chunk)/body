{
  for (int i=0; i < c._len; ++i) {
    if (!c.isNA(i)) {
      int h=(int)c.at8(i);
      long offset=ssid(h);
      long cnt=_cnts[h];
      while (!U.compareAndSwapLong(_cnts,offset,cnt,cnt + 1))       cnt=_cnts[h];
    }
  }
}
