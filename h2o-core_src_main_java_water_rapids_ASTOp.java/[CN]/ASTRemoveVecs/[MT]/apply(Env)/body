{
  int[] idxs=new int[_rmVecs.length];
  int i=0;
  for (  long l : _rmVecs)   idxs[i++]=(int)l;
  Frame fr=e.popAry();
  for (  Vec v : fr.remove(idxs))   v.remove();
  DKV.put(fr._key,fr);
  e.pushAry(fr);
}
