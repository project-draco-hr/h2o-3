{
  Frame fr=env.pop0Ary();
  String res="FALSE";
  for (int i=0; i < fr.vecs().length; ++i)   if (fr.vecs()[i].naCnt() > 0) {
    res="TRUE";
    break;
  }
  env.cleanup(fr);
  env.push(new ValStr(res));
}
