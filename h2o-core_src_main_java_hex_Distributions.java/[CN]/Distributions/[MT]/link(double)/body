{
switch (distribution) {
case AUTO:
case gaussian:
    return f;
case bernoulli:
  return log(f / (1 - f));
case multinomial:
case poisson:
case gamma:
case tweedie:
assert(0 < f);
return log(f);
default :
throw H2O.unimpl();
}
}
