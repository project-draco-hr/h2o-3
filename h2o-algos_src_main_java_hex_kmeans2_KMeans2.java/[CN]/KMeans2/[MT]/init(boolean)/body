{
  super.init(expensive);
  if (_parms._max_iters < 1 || _parms._max_iters > 9999999)   error("max_iters","must be between 1 and 10 million");
  if (_parms._K < 2 || _parms._K > 999999)   error("K","must be between 2 and a million");
}
