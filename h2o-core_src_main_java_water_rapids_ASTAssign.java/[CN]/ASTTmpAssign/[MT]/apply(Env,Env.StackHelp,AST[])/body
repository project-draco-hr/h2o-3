{
  Key id=Key.make(((ASTId)asts[1])._id);
  Frame src=stk.track(asts[2].exec(env)).getFrame();
  Frame dst=src.deepCopy(null);
  Keyed.remove(id);
  DKV.put(new Frame(id,dst._names,dst.vecs()));
  return env.addGlobals(dst);
}
