def echo_and_interact(demo_commands, interactive, echo, npop=2):
    if demo_commands:
        if echo:
            for p in range(npop):
                print(demo_commands.pop(0))
        if interactive:
            raw_input('Press ENTER...\n')
