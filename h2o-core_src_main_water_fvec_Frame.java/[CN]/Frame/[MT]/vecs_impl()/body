{
  if (_keys == null)   System.out.println("crunk");
  final Vec[] vecs=new Vec[_keys.length];
  for (int i=0; i < _keys.length; i++) {
    DKV.prefetch(_keys[i]);
    throw H2O.unimpl();
  }
  return vecs;
}
