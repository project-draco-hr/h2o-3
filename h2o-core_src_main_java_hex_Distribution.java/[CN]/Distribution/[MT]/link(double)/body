{
switch (distribution) {
case AUTO:
case gaussian:
case huber:
case laplace:
case quantile:
    return f;
case modified_huber:
case bernoulli:
  return log(f / (1 - f));
case multinomial:
case poisson:
case gamma:
case tweedie:
return log(f);
default :
throw H2O.unimpl();
}
}
