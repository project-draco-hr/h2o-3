{
switch (distribution) {
case AUTO:
case gaussian:
case huber:
case laplace:
case quantile:
    return f;
case bernoulli:
  return 1 / (1 + exp(-f));
case multinomial:
case poisson:
case gamma:
case tweedie:
return exp(f);
default :
throw H2O.unimpl();
}
}
