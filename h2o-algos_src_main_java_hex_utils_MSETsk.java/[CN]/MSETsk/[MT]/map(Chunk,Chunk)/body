{
  for (int i=0; i < prediction._len; ++i) {
    if (prediction.isNA0(i) || response.isNA0(i))     continue;
    double diff=prediction.at0(i) - response.at0(i);
    _resDev+=diff * diff;
    ++_nobs;
  }
}
