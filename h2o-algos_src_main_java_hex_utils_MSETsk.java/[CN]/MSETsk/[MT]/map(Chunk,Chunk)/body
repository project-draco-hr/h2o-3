{
  for (int i=0; i < prediction._len; ++i) {
    if (prediction.isNA(i) || response.isNA(i))     continue;
    double diff=prediction.atd(i) - response.atd(i);
    _resDev+=diff * diff;
    ++_nobs;
  }
}
