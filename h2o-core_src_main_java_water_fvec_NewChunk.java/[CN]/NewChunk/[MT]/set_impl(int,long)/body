{
  if (_ds != null)   return set_impl(i,(double)l);
  if (sparseLen() != len()) {
    int idx=Arrays.binarySearch(_id,0,sparseLen(),i);
    if (idx >= 0)     i=idx;
 else     cancel_sparse();
  }
  _ls[i]=l;
  _xs[i]=0;
  _naCnt=-1;
  return true;
}
