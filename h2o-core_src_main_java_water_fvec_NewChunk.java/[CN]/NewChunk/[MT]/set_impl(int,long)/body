{
  if (_ds != null)   return set_impl(i,(double)l);
  if (len() != len2()) {
    int idx=Arrays.binarySearch(_id,0,len(),i);
    if (idx >= 0)     i=idx;
 else     cancel_sparse();
  }
  _ls[i]=l;
  _xs[i]=0;
  return true;
}
