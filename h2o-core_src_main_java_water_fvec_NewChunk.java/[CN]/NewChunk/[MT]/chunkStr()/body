{
  final byte[] strbuf=Arrays.copyOf(_ss,_sslen);
  final byte[] idbuf=MemoryManager.malloc1(len2() * 4,true);
  for (int i=0; i < len2(); ++i)   UnsafeUtils.set4(idbuf,4 * i,_is[i]);
  return new CStrChunk(strbuf,idbuf,len2());
}
