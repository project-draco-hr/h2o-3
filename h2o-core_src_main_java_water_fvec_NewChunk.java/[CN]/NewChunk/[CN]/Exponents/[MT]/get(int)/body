{
  if (_vals1 != null) {
    int x=_vals1[id];
    if (x == Byte.MIN_VALUE)     x=Integer.MIN_VALUE;
 else     if (x == Byte.MIN_VALUE + 1)     x=Integer.MIN_VALUE + 1;
    return x;
  }
  return _vals4[id];
}
