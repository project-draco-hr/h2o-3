{
  if (_vals1 != null) {
    byte b=(byte)x;
    if (x == b && b > Byte.MIN_VALUE - 1) {
      addRaw(b);
      return;
    }
 else {
      int len=_vals1.length;
      if (_vals1.length == _c)       len=2 * len;
      _vals4=MemoryManager.malloc4(len);
      for (int i=0; i < _c; ++i)       _vals4[i]=_vals1[i];
      _vals1=null;
    }
  }
  addRaw(x);
}
