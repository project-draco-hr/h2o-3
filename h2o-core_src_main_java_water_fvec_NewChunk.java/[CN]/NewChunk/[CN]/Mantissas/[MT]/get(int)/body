{
  if (_vals1 != null) {
    long l=_vals1[id];
    return (l == Byte.MAX_VALUE) ? Long.MAX_VALUE : l;
  }
  if (_vals4 != null) {
    long l=_vals4[id];
    return (l == Integer.MAX_VALUE) ? Long.MAX_VALUE : l;
  }
  return _vals8[id];
}
