{
  int len=Math.max(_vals1 == null ? 0 : _vals1.length,_vals4 == null ? 0 : _vals4.length);
  _vals8=MemoryManager.malloc8(len);
  if (_c > 0) {
    if (_vals1 != null)     for (int i=0; i < _c; ++i)     _vals8[i]=_vals1[i];
 else     if (_vals4 != null) {
      for (int i=0; i < _c; ++i)       _vals8[i]=_vals4[i];
    }
  }
}
