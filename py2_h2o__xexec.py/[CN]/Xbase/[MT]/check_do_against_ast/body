def check_do_against_ast(self):
    if (self.execExpr != self.ast):
        raise Exception(('late binding to string should get same results as that during __init__?? %s %s' % (self.execExpr, self.ast)))
