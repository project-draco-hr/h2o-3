{
  if (f > MAX_EXP)   return (label - 1) * _curLearningRate;
 else   if (f < -MAX_EXP)   return (label - 0) * _curLearningRate;
 else   return (label - _expTable[(int)((f + MAX_EXP) * (EXP_TABLE_SIZE / MAX_EXP / 2))]) * _curLearningRate;
}
