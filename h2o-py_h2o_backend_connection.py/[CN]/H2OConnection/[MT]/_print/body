def _print(self, msg, flush=False, end=u'\n'):
    u'Helper function to print connection status messages when in verbose mode.'
    if (not self._verbose):
        return
    print2(msg, end=end, flush=flush)
