{
  int l1=winWord * _wordVecSize;
  for (int k=0; k < _wordVecSize; k++)   neu1e[k]=0;
  if (_normModel == NormModel.NegSampling)   negSamplingSG(curWord,l1,null,neu1e);
 else   hierarchicalSoftmaxSG(curWord,l1,neu1e);
  for (int i=0; i < _wordVecSize; i++)   _syn0[i + l1]+=neu1e[i];
}
