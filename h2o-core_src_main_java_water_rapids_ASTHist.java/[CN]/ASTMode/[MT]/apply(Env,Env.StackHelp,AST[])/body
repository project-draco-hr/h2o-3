{
  Frame fr=stk.track(asts[1].exec(env)).getFrame();
  if (fr.numCols() != 1 || !fr.anyVec().isEnum())   throw new IllegalArgumentException("mean only works on a single categorical column");
  return new ValNum(mode(fr.anyVec()));
}
