{
  if (_job != null) {
    _job.cancel();
    parseCleanup();
    _job._mfpt=null;
    if (ex instanceof H2OParseException)     throw (H2OParseException)ex;
 else     _job.failed(ex);
  }
  return true;
}
