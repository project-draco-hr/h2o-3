{
  if (_job != null) {
    parseCleanup();
    _job._mfpt=null;
    if (ex instanceof H2OParseException) {
      _job.cancel();
      throw (H2OParseException)ex;
    }
 else     _job.failed(ex);
  }
  return true;
}
