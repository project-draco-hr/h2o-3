{
switch (type) {
case json:
    return new Response(HTTP_OK,MIME_JSON,new String(S.writeJSON(new AutoBuffer()).buf()));
case xml:
case html:
case java:
  throw H2O.unimpl();
default :
throw H2O.fail();
}
}
