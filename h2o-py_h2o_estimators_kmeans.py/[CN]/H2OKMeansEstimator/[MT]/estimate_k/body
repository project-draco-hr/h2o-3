@property
def estimate_k(self):
    u'\n        bool: Whether to estimate the number of clusters (<=k) iteratively and deterministically. (Default: False)\n        '
    return self._parms.get(u'estimate_k')
