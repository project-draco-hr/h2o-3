{
  int mode=ab.get1();
  if (mode == 0)   return;
  K key;
  V val;
  while ((key=((mode == 1 || mode == 2) ? (K)ab.getStr() : (K)ab.get())) != null) {
    val=((mode == 1 || mode == 3) ? (V)ab.getStr() : (V)ab.get());
    _map.put(key,val);
  }
}
