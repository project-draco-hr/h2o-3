{
  long tmp[]=_counts._val[chk.cidx()]=new long[256];
  int shift=_biggestBit - 8;
  if (shift < 0)   shift=0;
  for (int r=0; r < chk._len; r++) {
    tmp[(int)(chk.at8(r) >> shift & 0xFFL)]++;
  }
}
