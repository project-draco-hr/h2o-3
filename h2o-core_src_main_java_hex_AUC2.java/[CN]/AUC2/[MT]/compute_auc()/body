{
  long tp0=0, fp0=0;
  double area=0;
  for (int i=0; i < _nBins; i++) {
    area+=tp0 * (_fps[i] - fp0);
    area+=(_tps[i] - tp0) * (_fps[i] - fp0) / 2.0;
    tp0=_tps[i];
    fp0=_fps[i];
  }
  return area / _at / _af;
}
