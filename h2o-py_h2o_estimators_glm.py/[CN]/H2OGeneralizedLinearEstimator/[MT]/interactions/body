@property
def interactions(self):
    u'\n        List[str]: A list of predictor column indices to interact. All pairwise combinations will be computed for the\n        list.\n        '
    return self._parms.get(u'interactions')
