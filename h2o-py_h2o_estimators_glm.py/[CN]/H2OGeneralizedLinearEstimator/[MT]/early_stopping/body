@property
def early_stopping(self):
    u'\n        bool: stop early when there is no more relative improvement on train or validation (if provided) (Default: True)\n        '
    return self._parms.get(u'early_stopping')
