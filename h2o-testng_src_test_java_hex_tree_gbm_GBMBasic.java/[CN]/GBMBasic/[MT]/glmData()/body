{
  Object[][] data=null;
  try {
    List<String> lines=Files.readAllLines(Paths.get("/Users/ece/0xdata/h2o-dev/h2o-testng/src/test/" + "resources/glmData.csv"),Charset.defaultCharset());
    data=new Object[lines.size()][5];
    int r=0;
    for (    String line : lines) {
      int c=0;
      for (      String variable : line.trim().split(",")) {
        data[r][c]=variable;
        c++;
      }
      r++;
    }
  }
 catch (  Exception ignore) {
  }
  return data;
}
