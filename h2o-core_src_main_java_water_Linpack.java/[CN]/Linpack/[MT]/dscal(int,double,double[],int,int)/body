{
  int i, nincx;
  if (n > 0) {
    if (incx != 1) {
      nincx=n * incx;
      for (i=0; i < nincx; i+=incx)       dx[i + dx_off]*=da;
    }
 else {
      for (i=0; i < n; i++)       dx[i + dx_off]*=da;
    }
  }
}
