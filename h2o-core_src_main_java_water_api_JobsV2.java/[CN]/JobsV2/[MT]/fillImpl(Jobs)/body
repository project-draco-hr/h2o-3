{
  Job[] jobs=null;
  if (null != this.jobs) {
    jobs=new Job[this.jobs.length];
    for (int i=0; i < this.jobs.length; i++)     jobs[i]=this.jobs[i].createImpl();
  }
 else {
    jobs=new Job[0];
  }
  super.fillImpl(j);
  return j;
}
