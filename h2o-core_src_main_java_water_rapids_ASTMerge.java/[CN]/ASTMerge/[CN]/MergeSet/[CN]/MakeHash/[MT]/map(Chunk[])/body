{
  int len=chks[0]._len;
  for (int i=0; i < _ncols; i++) {
    if (chks[i].vec().isEnum() || chks[i].vec().isString())     throw H2O.unimpl();
  }
  for (int i=0; i < len; i++) {
    boolean added=_rows.add(new Row(chks).fill(i,_ncols));
    if (!added)     throw H2O.unimpl();
  }
}
