{
  _row=row;
  _enum_maps=enum_maps;
  long hash=0;
  for (int i=0; i < ncols; i++)   hash+=enum_maps[i] == null ? Double.doubleToLongBits(_chks[i].atd(row)) : enum_maps[i][(int)(_chks[i].at8(row))];
  _hash=(int)(hash ^ (hash >> 32));
  return this;
}
