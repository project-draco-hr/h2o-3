{
  if (_bitoff != 0)   throw H2O.unimpl();
  StringBuilder sb=new StringBuilder();
  sb.append("{").append(_val[0]);
  int bytes=bytes(_nbits);
  for (int i=1; i < bytes; i++)   sb.append(", ").append(_val[i]);
  sb.append("}");
  return sb.toString();
}
