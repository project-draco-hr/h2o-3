def is_rbooklet(file_name):
    '\n    Return True if file_name matches a regexp for an R booklet.  False otherwise.\n    '
    if re.match('^rbooklet.*\\.[rR]$', file_name):
        return True
    return False
