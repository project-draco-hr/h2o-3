def num_iterations(self):
    u'Get the number of iterations that it took to converge or reach max iterations.\n\n        Returns\n        -------\n          Number of iterations (integer)\n        '
    o = self._model_json[u'output']
    return o[u'model_summary'].cell_values[0][o[u'model_summary'].col_header.index(u'number_of_iterations')]
