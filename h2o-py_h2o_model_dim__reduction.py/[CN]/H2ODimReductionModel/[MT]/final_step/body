def final_step(self):
    u'Get the final step size from the GLRM model.\n\n        Returns\n        -------\n          Final step size\n        '
    o = self._model_json[u'output']
    return o[u'model_summary'].cell_values[0][o[u'model_summary'].col_header.index(u'final_step_size')]
