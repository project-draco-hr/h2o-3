def objective(self):
    u'Get the final value of the objective function from the GLRM model.\n\n      Returns\n      -------\n        Final objective value\n      '
    o = self._model_json[u'output']
    return o[u'model_summary'].cell_values[0][o[u'model_summary'].col_header.index(u'final_objective_value')]
