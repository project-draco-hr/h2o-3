def __init__(self, json_raw=None):
    if (not json_raw):
        raise ValueError('Missing Data: Cannot instantiate a new H2OBinomialModelMetrics object.')
    self.auc_data = AUCData(json_raw['auc'])
