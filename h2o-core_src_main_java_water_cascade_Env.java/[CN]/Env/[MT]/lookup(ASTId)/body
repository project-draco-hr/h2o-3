{
switch (getType(id.value(),true)) {
case NUM:
    return new ASTNum(Double.valueOf(getValue(id.value(),true)));
case ARY:
  return new ASTFrame(id.value());
case STR:
return new ASTString('\"',id.value());
default :
throw H2O.fail("Could not find appropriate node for identifier " + id);
}
}
