{
  _local.clear();
  Futures fs=new Futures();
  for (  Vec v : _refcnt.keySet()) {
    if (!_locked.contains(v._key))     fs=removeVec(v,fs);
    extinguishCounts(v);
  }
  fs.blockForPending();
  _stack.popAll();
}
