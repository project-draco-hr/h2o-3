{
  String res=null;
  if (_local != null)   res=_local.typeOf(name);
  if (res == null && search_global)   res=_global.typeOf(name);
  if (res == null)   res=_parent.getType(name,false);
  if (res == null)   throw H2O.fail("Failed lookup of variable: " + name);
  return res;
}
