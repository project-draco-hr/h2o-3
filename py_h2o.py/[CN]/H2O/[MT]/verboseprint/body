@classmethod
def verboseprint(*args, **kwargs):
    if H2O.verbose:
        for x in args:
            print x,
        for x in kwargs:
            print x,
        print 
        sys.stdout.flush()
