def create_frame(self, timeoutSecs=180, **kwargs):
    a = self.__do_json_request('3/CreateFrame.json', timeout=timeoutSecs, params=kwargs)
    H2O.verboseprint('\ncreate_frame result:', h2o_util.dump_json(a))
    return a
