{
  if (_con == 0) {
    nc._id=new int[0];
    nc._ls=new long[0];
    nc._xs=new int[0];
  }
 else {
    nc._ls=MemoryManager.malloc8(_len);
    Arrays.fill(nc._ls,_con);
    nc._xs=MemoryManager.malloc4(_len);
  }
  return nc;
}
