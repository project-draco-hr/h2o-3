{
  nc.set_len(nc.set_len2(0));
  if (_con == 0) {
    nc.addZeros(len());
  }
 else {
    for (int i=0; i < len(); ++i)     nc.addNum(_con);
  }
  assert(nc.len() == len());
  assert(nc.len2() == len());
  return nc;
}
