{
  if (null == key)   return (S)this;
  this.name=key.toString();
  this.type="Key<" + this.getKeyedClassType() + ">";
  if ("Keyed".equals(this.type)) {
    String vc=key.valueClassSimple();
    if (null != vc) {
      this.type="Key<" + vc + ">";
    }
  }
  if (Job.class.isAssignableFrom(this._impl_class))   this.URL="/" + Schema.getHighestSupportedVersion() + "/Jobs.json/"+ key.toString();
 else   if (Frame.class.isAssignableFrom(this._impl_class))   this.URL="/" + Schema.getHighestSupportedVersion() + "/Frames.json/"+ key.toString();
 else   if (Model.class.isAssignableFrom(this._impl_class))   this.URL="/" + Schema.getHighestSupportedVersion() + "/Models.json/"+ key.toString();
 else   if (Vec.class.isAssignableFrom(this._impl_class))   this.URL=null;
 else   this.URL=null;
  return (S)this;
}
