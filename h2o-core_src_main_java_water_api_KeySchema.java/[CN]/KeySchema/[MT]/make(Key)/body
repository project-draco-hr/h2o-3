{
  if (null == key)   return null;
  Value v=DKV.get(key);
  if (null == v)   return new KeySchema();
  if (v.isFrame())   return make(KeyV1.FrameKeyV1.class,key);
 else   if (v.isModel())   return make(KeyV1.ModelKeyV1.class,key);
 else   if (v.isVec())   return make(KeyV1.VecKeyV1.class,key);
 else   if (v.isJob())   return make(KeyV1.JobKeyV1.class,key);
 else   return make(KeySchema.class,key);
}
