{
  Icer f=goForGold(id);
  if (f == null) {
    Class clz=null;
    try {
      clz=Class.forName(className(id));
    }
 catch (    Exception e) {
      Log.throwErr(e);
    }
    f=getIcer(id,clz);
  }
  return f.newFreezable();
}
