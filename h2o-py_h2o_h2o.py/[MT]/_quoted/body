def _quoted(key, replace=True):
    if (key == None):
        return '""'
    key = key.replace('%', '.')
    key = key.replace('&', '.')
    is_quoted = (len(re.findall('\\"(.+?)\\"', key)) != 0)
    key = (key if is_quoted else (('"' + key) + '"'))
    return key
