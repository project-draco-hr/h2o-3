def ls():
    u'List Keys on an H2O Cluster\n\n    Returns\n    -------\n      A list of keys in the current H2O instance.\n    '
    return H2OFrame._expr(expr=ExprNode(u'ls')).as_data_frame(use_pandas=True)
