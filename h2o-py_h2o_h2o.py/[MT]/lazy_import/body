def lazy_import(path):
    u'\n    Import a single file or collection of files.\n\n    :param path: A path to a data file (remote or local).\n    '
    if is_listlike(path):
        return [_import(p)[0] for p in path]
    else:
        assert_is_str(path, u'path')
        return _import(path)
