def _import(path):
    j = H2OConnection.get_json(url_suffix='ImportFiles', path=path)
    if j['fails']:
        raise ValueError(((('ImportFiles of ' + path) + ' failed on ') + str(j['fails'])))
    return j['destination_frames']
