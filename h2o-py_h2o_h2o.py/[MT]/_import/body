def _import(path):
    j = H2OConnection.get_json(url_suffix=u'ImportFiles', path=path)
    if j[u'fails']:
        raise ValueError((((u'ImportFiles of ' + path) + u' failed on ') + str(j[u'fails'])))
    return j[u'destination_frames']
