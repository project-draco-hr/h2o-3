def assign(data, xid):
    if (data.frame_id == xid):
        ValueError(u'Desination key must differ input frame')
    data._ex = ExprNode(u'assign', xid, data)._eval_driver(False)
    data._ex._cache._id = xid
    data._ex._children = None
    return data
