def api(endpoint, data=None, json=None, filename=None):
    u'Perform a REST API request to a previously connected server.'
    _check_connection()
    return h2oconn.request(endpoint, data=data, json=json, filename=filename)
