def data_file(relative_path):
    u"Return absolute path to a file within the 'h2o' folder."
    assert_is_type(relative_path, str)
    h2o_dir = os.path.split(__file__)[0]
    return os.path.join(h2o_dir, relative_path)
