def set_timezone(tz):
    u'Set the Time Zone on the H2O Cloud\n\n    Parameters\n    ----------\n      tz : str\n        The desired timezone.\n    '
    ExprNode(u'setTimeZone', tz)._eager_scalar()
