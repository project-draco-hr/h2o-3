{
  URI metafileUri=URI.create(outputDir + "/" + METAFILE);
  PrintStream os=new PrintStream(persist.create(metafileUri.toString(),true));
  Collections.reverse(filenames);
  try {
    for (    String fname : filenames)     os.println(fname);
  }
  finally {
    FileUtils.close(os);
  }
}
