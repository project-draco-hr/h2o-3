{
  if (null == key)   return;
  Value v=DKV.get(key);
  if (null == v)   throw H2O.fail("Did not find key in DKV: " + key.toString());
  Iced ice=v.get();
  if (!(ice instanceof Frame))   throw H2O.fail("Expected a Frame for key: " + key.toString() + "; got a: "+ ice.getClass());
  frames=new Frame[1];
  frames[0]=(Frame)ice;
}
