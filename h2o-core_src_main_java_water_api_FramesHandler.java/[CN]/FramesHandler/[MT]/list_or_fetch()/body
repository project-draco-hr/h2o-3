{
  if (this.version != 2)   throw H2O.fail("list_or_fetch should not be routed for version: " + this.version + " of route: "+ this.route);
  if (null != key) {
    fetch();
  }
 else {
    list();
  }
}
