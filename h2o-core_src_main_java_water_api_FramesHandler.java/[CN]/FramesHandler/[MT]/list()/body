{
  if (null != key) {
    if (this.version == 2) {
      fetch();
      return;
    }
 else {
      throw H2O.fail("Did not expect a key for version: " + this.version + " of route: "+ this.route);
    }
  }
}
