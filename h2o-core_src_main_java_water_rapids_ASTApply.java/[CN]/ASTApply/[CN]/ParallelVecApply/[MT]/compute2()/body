{
  int nTasks;
  addToPendingCount((nTasks=_fr.numCols()) - 1);
  _applyTasks=new ParallelVecTask[nTasks];
  for (int i=0; i < nTasks; ++i)   (_applyTasks[i]=new ParallelVecTask(this,_FUN,_fr,i,_funArgs,_env)).compute2();
}
