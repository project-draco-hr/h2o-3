{
  if (!http_methods.contains(method))   throw new MalformedURLException("Bad HTTP method: " + method);
  if (!version_pattern.matcher(url).matches())   throw new MalformedURLException("URLs must begin with a /{version}/: " + url);
  this.method=method;
  this.url=url;
  this.path=null;
}
