def pubdev_2371():
    d = h2o.H2OFrame.from_python({'a': [3.2, 'NA', None, 'nan'], 'b': ['x', 'NA', None, ''], }, column_types=['numeric', 'string'])
    print(d)
    print(d.types)
    print((d == ''))
    assert ((d['b'] == None).sum() == 2)
    assert ((d['b'] != None).sum() == 2)
    assert (d[((d['b'] != None), 'a')].shape == (2, 1))
