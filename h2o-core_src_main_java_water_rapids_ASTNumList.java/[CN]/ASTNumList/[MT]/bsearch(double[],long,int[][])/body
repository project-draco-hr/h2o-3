{
  int lb=0, ub=bases.length;
  int m=(ub + lb) >> 1;
  do {
    if (v == bases[m]) {
      res[0]=new int[]{m};
      return;
    }
 else     if (v < bases[m])     ub=m;
 else     lb=m;
    m=(ub + lb) >> 1;
  }
 while (m != lb);
  res[1]=new int[]{lb,ub};
}
