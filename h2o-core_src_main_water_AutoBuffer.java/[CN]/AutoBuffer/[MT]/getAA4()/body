{
  _arys++;
  long xy=getZA();
  if (xy == -1)   return null;
  int x=(int)(xy >> 32);
  int y=(int)xy;
  int z=y == 0 ? 0 : getInt();
  int[][] ary=new int[x + y + z][];
  for (int i=x; i < x + y; i++)   ary[i]=getA4();
  return ary;
}
