def multi_dim_slice_rows_cmd(self, child):
    r = child._data[0]
    if isinstance(r, int):
        return ('#' + str(r))
    if isinstance(r, slice):
        return (((('(: #' + str(r.start)) + ' #') + str(r.stop)) + ')')
    if (r == 'null'):
        return '"null"'
    raise NotImplementedError
