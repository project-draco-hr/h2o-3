def multi_dim_slice_cols_cmd(self, child):
    if isinstance(self._left._data, list):
        return '"null"'
    elif isinstance(self._left._data, unicode):
        c = child._data[1]
        if isinstance(c, int):
            return ('#' + str(c))
        if isinstance(c, slice):
            return (((('(: #' + str(c.start)) + ' #') + str(c.stop)) + ')')
        if (c == '()'):
            return '()'
    raise NotImplementedError
