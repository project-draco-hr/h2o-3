def __init__(self, op='', *args):
    assert isinstance(op, str), op
    self._op = op
    self._children = tuple(((a._ex if isinstance(a, frame.H2OFrame) else a) for a in args))
    self._cache = H2OCache()
