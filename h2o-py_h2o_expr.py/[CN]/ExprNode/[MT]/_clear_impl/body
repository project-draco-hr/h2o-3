def _clear_impl(self):
    if (not isinstance(self._ast, tuple)):
        return
    for ast in self._ast:
        if isinstance(ast, ExprNode):
            ast._clear_impl()
    if self._id:
        self._ast = True
