import sys
sys.path.insert(1, '..')
import h2o
if (__name__ == '__main__'):
    args = sys.argv
    print args
    if (len(args) > 1):
        deep_learning_metrics_test(args[1], int(args[2]))
    else:
        deep_learning_metrics_test('localhost', 54321)
