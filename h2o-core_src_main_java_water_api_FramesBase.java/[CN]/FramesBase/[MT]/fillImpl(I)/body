{
  super.fillImpl(f);
  if (null != frames) {
    f.frames=new Frame[frames.length];
    int i=0;
    for (    FrameBase frame : this.frames) {
      f.frames[i++]=frame._fr;
    }
  }
  return f;
}
