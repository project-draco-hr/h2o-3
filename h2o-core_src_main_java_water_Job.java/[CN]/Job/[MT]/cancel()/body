{
  if (!_cancel_requested)   new JAtomic(){
    @Override boolean abort(    Job job){
      return job._cancel_requested;
    }
    @Override void update(    Job job){
      job._cancel_requested=true;
    }
  }
;
}
