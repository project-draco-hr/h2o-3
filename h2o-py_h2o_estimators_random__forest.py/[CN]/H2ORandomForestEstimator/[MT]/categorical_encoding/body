@property
def categorical_encoding(self):
    u'\n        Enum["auto", "enum", "one_hot_internal", "one_hot_explicit", "binary", "eigen"]: Encoding scheme for categorical\n        features (Default: "auto")\n        '
    return self._parms.get(u'categorical_encoding')
