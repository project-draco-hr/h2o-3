@property
def nbins(self):
    u'\n        int: For numerical columns (real/int), build a histogram of (at least) this many bins, then split at the best\n        point (Default: 20)\n        '
    return self._parms.get(u'nbins')
