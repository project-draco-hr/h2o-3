@property
def keep_cross_validation_predictions(self):
    u'bool: Whether to keep the predictions of the cross-validation models. (Default: False)'
    return self._parms.get(u'keep_cross_validation_predictions')
