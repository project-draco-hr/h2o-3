@property
def stopping_tolerance(self):
    u'\n        float: Relative tolerance for metric-based stopping criterion (stop if relative improvement is not at least this\n        much) (Default: 0.001)\n        '
    return self._parms.get(u'stopping_tolerance')
