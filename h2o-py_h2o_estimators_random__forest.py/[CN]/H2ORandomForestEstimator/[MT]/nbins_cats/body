@property
def nbins_cats(self):
    u'\n        int: For categorical columns (factors), build a histogram of this many bins, then split at the best point.\n        Higher values can lead to more overfitting. (Default: 1024)\n        '
    return self._parms.get(u'nbins_cats')
