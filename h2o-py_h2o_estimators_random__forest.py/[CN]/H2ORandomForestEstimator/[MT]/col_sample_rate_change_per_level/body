@property
def col_sample_rate_change_per_level(self):
    u'float: Relative change of the column sampling rate for every level (from 0.0 to 2.0) (Default: 1.0)'
    return self._parms.get(u'col_sample_rate_change_per_level')
