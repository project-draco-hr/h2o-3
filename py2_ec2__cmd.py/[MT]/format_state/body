def format_state(state):
    if (state == 'stopped'):
        return '\x1b[093mSTOPPED\x1b[0m'
    if (state == 'running'):
        return '\x1b[092mRUNNING\x1b[0m'
    if (state == 'terminated'):
        return '\x1b[090mTERMINATED\x1b[0m'
    return state.upper()
