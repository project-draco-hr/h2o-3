def __repr__(self):
    x = self.eager()
    if self.isLocal():
        return x.__str__()
    print ('Download ' + str(x))
    frames = H2OCONN.Frame(x)
    print frames
    raise NotImplementedError
