def __repr__(self):
    x = self.eager()
    if self.isLocal():
        return x.__str__()
    global _CMD
    print ('Download ' + str(_CMD))
    frames = H2OCONN.Frame(x)
    print frames
    raise NotImplementedError
