def __del__(self):
    if (self._data is None):
        print 'DELE: Expr never evaluated:', self._name
    if isinstance(self._data, list):
        global _CMD
        s = (('DELE: ' + self._name) + '; ')
        if _CMD:
            _CMD += s
        else:
            print s
