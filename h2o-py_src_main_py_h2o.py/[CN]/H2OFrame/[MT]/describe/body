def describe(self):
    print 'Rows:', len(self._vecs[0]), 'Cols:', len(self)
    headers = [vec._name for vec in self._vecs]
    table = [_row('missing', self._vecs, None), _row('mean', self._vecs, None), _row('sigma', self._vecs, None), _row('zeros', self._vecs, None), _row('mins', self._vecs, 0), _row('maxs', self._vecs, 0)]
    print tabulate(table, headers)
