def __delitem__(self, i):
    if isinstance(i, str):
        for v in self._vecs:
            if (i == v._name):
                self._vecs.remove(v)
                return self
        raise KeyError((('Name ' + i) + ' not in Frame'))
    raise NotImplementedError
