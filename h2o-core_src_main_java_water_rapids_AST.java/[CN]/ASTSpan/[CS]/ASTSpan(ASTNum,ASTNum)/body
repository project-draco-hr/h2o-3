{
  _ast_min=min;
  _ast_max=max;
  _min=(long)min._d;
  _max=max._d;
  if (_min <= 0 && _max <= 0) {
    if (!Double.isNaN(_max) && _max > _min)     throw new IllegalArgumentException("max>min: All negative, incorrect order.");
  }
 else {
    if (!Double.isNaN(_max) && _min > _max)     throw new IllegalArgumentException("min > max: min <= max for `:` operator.");
  }
}
