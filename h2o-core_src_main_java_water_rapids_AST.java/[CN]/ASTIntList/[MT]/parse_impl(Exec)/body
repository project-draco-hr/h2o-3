{
  ArrayList<Integer> ints=new ArrayList<>();
  while (!E.isEnd())   ints.add((int)E.nextDbl());
  E.eatEnd();
  _i=new int[ints.size()];
  int j=0;
  for (  int i : ints)   _i[j++]=i;
  ASTIntList res=(ASTIntList)clone();
  res._i=_i;
  return res;
}
