{
  ArrayList<Float> flts=new ArrayList<>();
  while (!E.isEnd())   flts.add((float)E.nextDbl());
  E.eatEnd();
  _f=new float[flts.size()];
  int i=0;
  for (  float f : flts)   _f[i++]=f;
  ASTFloatList res=(ASTFloatList)clone();
  res._f=_f;
  return res;
}
