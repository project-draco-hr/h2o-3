{
  ArrayList<String> strs=new ArrayList<>();
  while (!E.isEnd())   strs.add(E.nextStr());
  E.eatEnd();
  _s=new String[strs.size()];
  int i=0;
  for (  String s : strs)   _s[i++]=s;
  ASTStringList res=(ASTStringList)clone();
  res._s=_s;
  return res;
}
