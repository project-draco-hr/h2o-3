{
  ArrayList<AST> ast_ary=new ArrayList<AST>();
  while (!E.isEnd())   ast_ary.add(E.parse());
  E.eatEnd();
  ASTStatement res=(ASTStatement)clone();
  res._asts=ast_ary.toArray(new AST[ast_ary.size()]);
  return res;
}
