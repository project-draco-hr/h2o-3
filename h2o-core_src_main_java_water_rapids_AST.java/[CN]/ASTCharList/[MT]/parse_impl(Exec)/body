{
  ArrayList<Character> chars=new ArrayList<>();
  while (!E.isEnd())   chars.add(E.nextStr().charAt(0));
  E.eatEnd();
  _c=new char[chars.size()];
  int j=0;
  for (  char c : chars)   _c[j++]=c;
  ASTCharList res=(ASTCharList)clone();
  res._c=_c;
  return res;
}
