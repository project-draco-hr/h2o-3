{
  ArrayList<Byte> bytes=new ArrayList<>();
  while (!E.isEnd())   bytes.add((byte)E.nextDbl());
  E.eatEnd();
  _b=new byte[bytes.size()];
  int j=0;
  for (  byte b : bytes)   _b[j++]=b;
  ASTByteList res=(ASTByteList)clone();
  res._b=_b;
  return res;
}
